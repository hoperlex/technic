-- Запись журнала обновлений (ADR 0077): выпуск 89 — сторож печати путевого листа (ADR 0178, план
-- `docs/vehicle-request-actual-end-date-plan.md`, Р21, этап Э0).
--
-- ОТКУДА НОМЕР. `ls apps/api/drizzle/*.sql | tail` перед созданием файла (10.09.2026): последней в
-- дереве стоит `0300_service_request_executor_scope_flag.sql` соседнего потока, свободны 0301 и
-- 0302. Проверены и рабочие копии соседних веток (`git worktree list`): дальше `0291` там ничего
-- нет. Номер спрашивается у ФАЙЛОВ дерева, а не у журнала базы: на technic_dev часть закоммиченных
-- миграций не накатана, и `max(seq)` там врёт в меньшую сторону.
--
-- `seq = 89`, И ЭТО ПЕРВЫЙ НЕЗАНЯТЫЙ. Номер спрошен у РЕПОЗИТОРИЯ
-- (`grep -h "^  [0-9]\+, '" apps/api/drizzle/*releases*.sql`), а не у базы. Лента идёт до 88
-- (`0.1.77.0177`, предмет заявки оргтехники). Версия `0.1.78.0178`: счётчик идёт дальше от 77,
-- хвост — номер решения выпуска.
--
-- ПОЧЕМУ У СТОРОЖА СВОЯ ЗАПИСЬ, А НЕ СТРОКА В ОБЩЕЙ. Он самодостаточен и уезжает сам по себе, до
-- всей остальной волны (§5 плана, выпуск 1): он чинит существующую гонку печати и полезен
-- независимо от судьбы правки листов. Главное же в том, что печать начинает **отказывать** там,
-- где раньше молча отдавала файл, — а про новый отказ человек обязан прочесть в ленте выпусков, а
-- не гадать, почему пачка вдруг не печатается. Сложи мы это в общую запись волны, отказ приехал бы
-- на прод раньше собственного объяснения.
--
-- ЧТО ЭТА ЗАПИСЬ ОБЕЩАЕТ И ЧЕГО НЕ ОБЕЩАЕТ. Обещано: файл, собранный по устаревшему листу, наружу
-- не уйдёт — портал попросит открыть печать заново, и в журнале печати остаётся версия листа,
-- которая ушла на площадку. НЕ обещано, что окно закрыто целиком: между сверкой и отправкой файла
-- остаётся промежуток в миллисекунды (ADR 0101 п. 18) — убрана длинная его часть, сборка PDF. И не
-- обещано, что сторож уже что-то ловит: пока ни одна правка не поднимает версию листа, ловить ему
-- нечего — работать он начнёт вместе с сокращением периода из той же волны (Р21, находка Э0).
--
-- ОБРАТИМОСТЬ: строка ленты, `DELETE FROM app_releases WHERE seq = 89`.

INSERT INTO app_releases (seq, version, released_on, title, adrs, items) VALUES (
  89, '0.1.78.0178', '2026-09-10', 'Печать путевого листа сверяет, что лист не изменился', '{178}',
  '[
    {"kind":"fix","text":"Печать и выгрузка путевого листа перед отдачей файла перечитывают лист заново. Если за то время, пока собирался документ, лист аннулировали или поправили, портал скажет «лист изменился, пока готовился файл: откройте печать заново» и назовёт номера — вместо того чтобы отдать устаревший бланк, неотличимый от действующего"},
    {"kind":"improvement","text":"В журнале печати рядом с номером листа теперь записана его версия — и при одиночной печати, и при пакетной. По ней видно, какой именно вариант бланка ушёл на площадку"}
  ]'::jsonb
);
