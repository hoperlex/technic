-- Новый тип заявки: «Снятие контейнера» (окончательный вывоз контейнера с площадки).
-- Значение enum добавляется отдельной миграцией: его нельзя использовать (напр. во view 0007)
-- в той же транзакции, где оно добавлено.
ALTER TYPE request_type ADD VALUE IF NOT EXISTS 'container_removal';
