-- Запись журнала обновлений (ADR 0077): выпуск 66 — заявку можно завести без аппарата.
--
-- Схему этого выпуска меняет соседняя `0230` (снятие `NOT NULL` с предмета и двусоставный
-- `service_requests_subject_check`). Выдачу права двум наборам несёт `0231`, и она НАМЕРЕННО уезжает
-- ОТДЕЛЬНЫМ КОММИТОМ И ОТДЕЛЬНЫМ ВЫКАТОМ — почему, сказано ниже.
--
-- Номер файла, `seq` и версия взяты по факту перед коммитом: на этот момент последняя миграция в
-- дереве — `0231`, последний выпуск — 65 (`0.1.56.0146`, вкладка расходников и закупка), отсюда файл
-- `0232` и `seq = 66`. Хвост версии — номер ADR решения (0146): он общий у всего пака, третий выпуск
-- того же решения его не меняет, а счётчик выпусков идёт дальше — `0.1.57`.
--
-- ПОЧЕМУ ВЫДАЧА ПРАВА ЕДЕТ ОТДЕЛЬНО. У этого выпуска есть порядок, который нельзя нарушить, и держит
-- его не дисциплина, а данные. Ответ о заявке стал несовместимым со старыми вкладками (`equipment` и
-- `object` теперь бывают пустыми, а сборки контракта 1 читают их без проверки), и защита от этого —
-- гейт версии клиента: пол `MIN_CLIENT_CONTRACT` поднимается до 2 и отбивает старые вкладки `426`.
-- Но пол можно поднять только ПОСЛЕ того, как раздаётся новая статика, то есть уже после выката.
-- В промежутке старая вкладка ещё жива — и заявка без аппарата, появись она в этот момент, уронила
-- бы ей список.
--
-- Промежуток закрывается тем, что в нём НЕКОМУ завести такую заявку: право
-- `serviceRequests.createWithoutEquipment` в словаре есть, а в наборах его ещё нет, потому что `0231`
-- не накатана. Порядок выката поэтому такой: этот выпуск → проверить, что раздаётся новая сборка →
-- поднять пол до 2 → и только потом накатить `0231`. Ровно так же поступал план расходников: набор
-- завели, а выдали позже.
--
-- ПРО ГЕЙТ СКАЗАНО ПУНКТОМ ЖУРНАЛА, ХОТЯ ЗАВЕДЁН ОН БЫЛ ВЫПУСКОМ 65. Тогда он молчал (пол равен
-- единице) и в журнал не попал намеренно: лента — это то, что человек заметит. Здесь он начинает
-- действовать, и человек заметит именно его: вкладка, открытая со вчера, вместо данных покажет
-- требование обновить страницу. Умолчать — значит получить обращение «портал сломался».
--
-- ПЕРВЫЙ ПУНКТ НАЗЫВАЕТ, КОМУ ЭТО ПОЛОЖЕНО, И СРАЗУ ЖЕ — КОМУ НЕТ. Иначе рядовой заявитель прочитает
-- «аппарат теперь необязателен» и решит, что у него сломалась форма.
INSERT INTO app_releases (seq, version, released_on, title, adrs, items) VALUES (
  66, '0.1.57.0146', '2026-09-01', 'Заявку можно завести без аппарата', '{146}',
  '[
    {"kind":"feature","text":"ИТ-служба и ведение оргтехники могут завести заявку, не выбирая аппарат: поле «Какой аппарат» у них перестало быть обязательным. Это для случаев, когда чинить нечего конкретного — настроить почту, подключить рабочее место, разобраться с доступом. У остальных заявителей поле обязательно по-прежнему: заявка про аппарат должна называть аппарат"},
    {"kind":"improvement","text":"Если аппарат не выбран, обязательным становится поле «Для кого заявка», и выбрать в нём можно только своё: у сотрудника площадки — его площадки, у сотрудника отдела — его отделы. ИТ-служба, которая видит модуль целиком, выбирает и то и другое. Правило не для порядка: заявка, записанная на чужую площадку, пропала бы у самого заводящего сразу после отправки. В списках и карточках такая заявка называется «Без аппарата», а строка «Где стоит» у неё не показывается — показывать нечего"},
    {"kind":"improvement","text":"Портал теперь сам замечает, что вкладка устарела, и требует обновить страницу, если открытая вкладка не понимает новых ответов сервера. Раньше в такой ситуации предлагалось обновиться кнопкой, которую можно было отложить, — и отложенная вкладка могла показать пустой экран вместо списка. Обычное предложение обновиться при новом выпуске никуда не делось и по-прежнему откладывается: требование появляется только тогда, когда работать со старой вкладкой действительно нельзя"}
  ]'::jsonb
);
