-- Роль «Наблюдатель» (ADR 0033): сквозной просмотр обоих модулей заявок без права что-либо
-- менять.
--
-- Только значение enum: ни колонок, ни инвариантов роль не добавляет. Область видимости у неё
-- не сужена — заявки всех объектов, — поэтому CHECK вида `users_rukstroy_object_check` (0050)
-- или `users_operator_counterparty_check` (0023) здесь не нужен: привязывать учётку не к чему.
-- Всё остальное — какие права у роли — живёт в матрице @technic/contracts (ADR 0021), а не в БД.
ALTER TYPE role ADD VALUE 'observer' AFTER 'operator';
