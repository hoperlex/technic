-- Запись журнала обновлений (ADR 0077): выпуск 78 — заявителю в карточке заявки больше не видно
-- денег (ADR 0160, план `docs/office-equipment-requester-card-plan.md`, выпуск A).
--
-- Схему этот файл не трогает: право `serviceRequests.finance` двум наборам каталога завела
-- соседняя `0259`, всё остальное в этой волне — код (проекция карточки, списка, истории, файлов и
-- карточки аппарата). Здесь только строка ленты.
--
-- ОТКУДА НОМЕРА. `ls apps/api/drizzle` перед созданием файла (03.09.2026): последняя миграция в
-- дереве — `0258_module_mail_event_settings.sql` соседнего потока (почтовое расширение модуля).
-- Свободны 0259 и 0260; первый занят каталогом этой же волны, этому файлу достался 0260.
--
-- `seq = 78`, И ЭТО ПЕРВЫЙ НЕЗАНЯТЫЙ. Номер спрошен у репозитория
-- (`grep -h "^  [0-9]" apps/api/drizzle/*releases*.sql`): сегодня номера 1…77 заняты ровно по
-- одному разу, дыр и повторов в ленте нет. Оговорка нужна потому, что двумя выпусками раньше
-- ответ был другим: 75 стоял одновременно в `0248` («Остаток автозапчастей больше не ведётся») и в
-- `0253` соседнего потока, и `0255` брала 77 через голову свободного 76, чтобы не отбирать номер у
-- соседа второй раз подряд (разбор — в её шапке). Ту двойную запись с тех пор поправили: `0253`
-- держит 76, `0255` — 77. Поэтому здесь берётся именно следующий по порядку, а не «следующий за
-- спорным»: спорных больше нет, и обходить нечего.
--
-- ВЕРСИЯ `0.1.67.0160`: счётчик идёт дальше от 66 (последняя строка ленты — 77, `0.1.66.0158`),
-- хвост — номер решения (0160).
--
-- ЧТО СКАЗАНО: ровно то, что человек увидит сам, открыв свою заявку, — вкладок стало три, цифр в
-- них нет, а «Документы» не опустели. И то, чего он сам не увидит, но обязан знать, чтобы не
-- счесть портал сломанным: акт и счёт закрыты намеренно, спорить о выполненных работах — в
-- обсуждении заявки, а у тех, кто заявку ведёт, всё осталось как было.
--
-- ЧЕГО В ЗАПИСИ НЕТ И ПОЧЕМУ: ни слова про право, аудитории и проекцию ответа сервера — это
-- устройство, а не то, что видно в портале; ни слова про прямую ссылку на файл, которая теперь
-- отвечает «не найдено», — человек по ней и не ходит, он нажимает на строку в карточке.
INSERT INTO app_releases (seq, version, released_on, title, adrs, items) VALUES (
  78, '0.1.67.0160', '2026-09-03', 'Оргтехника: заказчику не видно, во сколько обошёлся ремонт', '{160}',
  '[
    {"kind":"improvement","text":"Тому, кто завёл заявку на обслуживание оргтехники, больше не видно её денежной стороны: вкладки «Объём работ» в карточке нет вовсе, сумм в списке заявок тоже, а счёт и акт не открываются и не скачиваются. Ремонт печатной техники — расчёты компании с подрядчиком, и к предмету заявки «не работает принтер» они отношения не имеют"},
    {"kind":"improvement","text":"Заявителю остаются три вкладки — «Заявка», «История» и «Документы». Документы не пустеют: там его собственные вложения — фотографии поломки и всё, что он приложил сам, — и гарантийный талон, потому что обращаться по гарантии будет он. В истории по-прежнему видно всё, что с заявкой происходило: кого назначили, когда предъявили и согласовали объём работ, когда работы закрыли, — только без цен. Если по выполненным работам есть вопрос, его задают в обсуждении заявки"},
    {"kind":"improvement","text":"«Ведение», ИТ-служба и сервисная компания видят карточку целиком, как и раньше: смету со строками и ценами, итог, счёт и акт — ни одно поле у них не убрано. Суммы ремонтов в истории обслуживания аппарата и в её выгрузке в файл тоже остались, но теперь их видят только они: у заказчика в этой колонке пусто"}
  ]'::jsonb
);
