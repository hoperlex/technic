-- Запись журнала обновлений (ADR 0077): выпуск 90 — закрытие заказа техники фактической датой
-- (ADR 0178, план `docs/vehicle-request-actual-end-date-plan.md`, этап Э14, второй выпуск волны).
--
-- ОТКУДА НОМЕР. `ls apps/api/drizzle/*.sql | tail` перед созданием файла (10.09.2026): в дереве
-- последним стоит `0300_service_request_executor_scope_flag.sql` соседнего потока, 0301 занят
-- записью выпуска сторожа печати из этой же волны, свободен 0302. Рабочие копии соседних веток
-- (`git worktree list`) дальше `0291` не уходят. Номер спрашивается у ФАЙЛОВ дерева, а не у журнала
-- базы: на technic_dev часть закоммиченных миграций не накатана.
--
-- `seq = 90` — следующий за 89 (сторож печати, `0301`). Версия `0.1.79.0178`: счётчик идёт дальше
-- от 78, хвост — номер решения. Решение у обоих выпусков одно (ADR 0178) и это не описка: сторож
-- печати — его же пункт 10, просто уехавший раньше остальных, потому что полезен сам по себе.
--
-- ПОЧЕМУ ЗАПИСЬ ИДЁТ В САМОМ ВЫКАТЕ, А НЕ ПОСЛЕ НЕГО. Этим выпуском сервер и портал катятся парой
-- (Р1): старая статусная ручка перестаёт принимать «Выполнена» у заказа техники на объект, и
-- единственной дверью закрытия становится новое окно. Разъехавшаяся пара означала бы либо закрытие
-- мимо фактической даты, либо окно, которому некуда стучаться, — поэтому и запись о перемене едет
-- вместе с ней.
--
-- ЧТО ЭТА ЗАПИСЬ ОБЕЩАЕТ И ЧЕГО НЕ ОБЕЩАЕТ. Обещано: заказ закрывается тем днём, которым работы
-- кончились; срок сокращается по этот день; лист недели, в которую попал последний рабочий день,
-- правится по него, а не сжигается и выписывается заново. НЕ обещано, что распечатанный экземпляр
-- станет верным: графа «Период работы» на бумаге, лежащей у площадки, остаётся прежней и заведомо
-- шире факта (решение заказчика, ADR 0178 п. 2) — отработанное сверяют по заполненным строкам
-- бланка и по порталу. И не обещано, что откат вернёт срок: заявка возвращается в работу
-- сокращённой, а продление выписывает новый лист (ADR 0178 п. 12).
--
-- ПОРЯДОК ВЫКАТА: схема этой волны (`0292`) уже на проде; сервер и портал — одним выкатом; эта
-- запись — вместе с ними. Аварийный откат один: вернуть прежнюю пару «сервер + портал» целиком.
-- Половинчатого отката нет — при старом портале и новом сервере заказ техники закрыть нечем.
--
-- ОБРАТИМОСТЬ: строка ленты, `DELETE FROM app_releases WHERE seq = 90`.

INSERT INTO app_releases (seq, version, released_on, title, adrs, items) VALUES (
  90, '0.1.79.0178', '2026-09-10', 'Заказ техники закрывают фактической датой, а лист сокращают', '{178}',
  '[
    {"kind":"feature","text":"Окно закрытия заказа техники на объект спрашивает фактическую дату окончания работ: технику брали до воскресенья, а фронт закрылся в среду — закрывайте средой. Срок заявки сокращается до этого дня, и машина перестаёт числиться занятой на оставшиеся дни"},
    {"kind":"feature","text":"Лист ЭСМ-2 недели, в которую попал последний рабочий день, портал теперь сокращает по эту дату: номер тот же, состав тот же, площадка ничего не переподписывает. Раньше на это сгорал номер строгой отчётности и выписывался новый бланк за уже отработанные дни"},
    {"kind":"feature","text":"До нажатия окно показывает всё, что произойдёт: какие листы будут аннулированы, какие часы смен снимутся, какие дни уйдут из рейсов и какие решения о технике погаснут. Показанное подтверждается тем же нажатием — если за время просмотра что-то изменилось, портал попросит посмотреть последствия заново"},
    {"kind":"improvement","text":"Дни линейного заказа, которые он отработал, после закрытия остаются в своих рейсах: «чей это был выезд» читается и у закрытой заявки. Снимаются только дни за фактической датой — их у заказа больше нет"},
    {"kind":"improvement","text":"Досрочное завершение больше не обещает перевыписку листов на глазок: и тому, кто просит за себя, и тому, кто визирует чужой запрос, портал показывает посчитанные сервером числа — сколько листов сократится и по какое число, сколько аннулируется, какие дни уйдут из рейсов"},
    {"kind":"improvement","text":"В журнале путевых листов у сокращённого бланка стоит метка с подсказкой: до какого числа сокращён период и по какое напечатано в выданном экземпляре. Та же оговорка — у кнопки печати: повторная печать даёт тот же номер с укороченным периодом, а не копию листа с площадки"},
    {"kind":"fix","text":"Возврат выполненной заявки в работу больше не притворяется, что вернул срок: окно возврата прямо говорит, что срок останется сокращённым, а продлить его надо отдельно — продление выпишет новый лист"}
  ]'::jsonb
);
