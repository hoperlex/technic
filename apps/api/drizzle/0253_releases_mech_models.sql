-- Запись выпуска: модель механизации выбирают из справочника (ADR 0156, миграции 0249–0251).
--
-- Схему этого выпуска меняют соседние миграции: `0249` (таблица моделей), `0250` (наполнение,
-- 103 строки) и `0251` (ссылка в заявке плюс перенос по нормализованному написанию). Здесь только
-- запись для людей — тем же приёмом, что `0223` рядом с `0221`.
--
-- Номер файла, `seq` и версия взяты ПО ФАКТУ перед коммитом: на момент создания последняя миграция
-- в дереве — `0252` (чужая, ТТХ оргтехники). Хвост версии — номер ADR решения (0156).
--
-- `seq` ИСПРАВЛЕН С 75 НА 76 ПРИ ЭТАПЕ Э3, И ПРИЧИНА СТОИТ ТОГО, ЧТОБЫ ЕЁ ЗАПИСАТЬ. Сверка «какой
-- выпуск последний» шла по накатанным на деве, а дев отстаёт от прода: соседний, уже закоммиченный
-- `0248_releases_auto_parts_freeze.sql` держит `seq = 75` и на дев просто не накатывался. На чистой
-- базе (её и собирают db-тесты, применяя все миграции подряд) две записи с одним `seq` дают
-- `app_releases_pkey` — и падает не этот файл, а ВСЯ подготовка тестов, то есть каждый db-набор
-- репозитория разом. Отсюда правило на будущее: свободный `seq` берётся ПО ФАЙЛАМ ДЕРЕВА
-- (`grep 'INSERT INTO app_releases' -A 1 apps/api/drizzle/*.sql`), а не по таблице дева. Номер 77
-- к моменту правки уже занят соседней чужой работой (`0255`), 76 свободен — его и берём.
-- (Правка от 03.09.2026: в теле файла какое-то время стояло 78 — это оставляло в ленте дыру на
-- месте 76, хотя шапка обещала именно его. Номер приведён к обещанному.)
--
-- ЗАПИСИ У ПРЕДЫДУЩЕГО ЭТАПА НЕТ, И ЭТО НЕ ПРОПУСК. Справочник появился отдельным выкатом раньше,
-- но заявки его не использовали: показывать людям вкладку, которой ещё нельзя пользоваться, значит
-- обещать работающее. Лента получает одну запись — ту, где перемена видна в работе.
--
-- ПРО СТРОГОСТЬ СКАЗАНО ПРЯМО, А НЕ СМЯГЧЕНО. «Выбор из списка» звучит как удобство, но означает
-- запрет: модели нет в справочнике — заявку не завести. Умолчи об этом, и первый же человек с
-- незаведённой моделью решит, что портал сломался, а не что ему нужно к держателю справочников.
--
-- НОМЕР ВЫПУСКА ПРАВИЛСЯ ДВАЖДЫ, И ЭТО СЛЕД РЕАЛЬНОЙ ОШИБКИ. Сначала здесь стоял `seq = 75`, взятый
-- по деву — а на деве не была накатана закоммиченная `0248`, которая этот номер уже держит. На
-- чистой базе (её собирает каждый db-прогон) выходил конфликт первичного ключа, и падал ВЕСЬ набор
-- репозитория, а не только механизация. Отсюда правило: номер выпуска сверяется по файлам
-- `apps/api/drizzle/*.sql`, а не по журналу дева, который отстаёт.
--
-- ПРО СТАРЫЕ ЗАЯВКИ СКАЗАНО ОТДЕЛЬНО. Перенос сопоставлял написания, и совпало не всё: заявка с
-- обобщённым «Компрессор дизельный 5 м³/мин» не найдёт себе конкретной модели никогда. Такие
-- показываются прежним написанием и работают как раньше — иначе человек, открыв старую заявку,
-- увидел бы пустое место там, где вчера стояла техника.
INSERT INTO app_releases (seq, version, released_on, title, adrs, items) VALUES (
  76, '0.1.66.0156', '2026-09-03', 'Механизация: модель выбирают из справочника', '{156}',
  '[
    {"kind":"feature","text":"В заявке на механизацию модель больше не набирают строкой — её выбирают из справочника. Он уже наполнен: 103 позиции, которые вы прислали, от виброплит и компрессоров до станков и тепловых пушек. Отбор по модели в списках и в журнале теперь точный: раньше «Виброплита реверсивная» и «виброплита реверсивная» были для отбора разными словами, и часть заявок в него не попадала"},
    {"kind":"improvement","text":"Выбор строгий: если нужной модели в справочнике нет, заявку по ней не завести. Это не поломка — новые модели заводит тот, кому выдано ведение справочников, на вкладке «Модели механизации» в разделе «Справочники». Так справочник остаётся перечнем того, что действительно арендуют, а не свалкой написаний"},
    {"kind":"improvement","text":"Заявки, заведённые до этого выпуска, никуда не делись, но название техники у части из них пропало. Те, чьё написание совпало с моделью справочника, получили её автоматически — сравнение шло без оглядки на регистр и лишние пробелы. У остальных в столбце «Модель» теперь прочерк: обобщённые названия вроде «Компрессор дизельный 5 м³/мин» ни одной конкретной модели не соответствуют, придумывать её портал не стал, а хранить старое написание рядом со справочником больше негде. Такие заявки по-прежнему открываются, правятся и закрываются; поиск по названию их не находит — искать их нужно по номеру, площадке или комментарию"}
  ]'::jsonb
);
