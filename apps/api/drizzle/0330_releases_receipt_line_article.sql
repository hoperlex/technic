-- Запись выпуска: артикул в строке чека на автозапчасти.
--
-- Решение — docs/adr/0202-auto-part-receipt-article.md. Само поле заводит миграция
-- 0329_receipt_line_article.sql; здесь только лента выпусков.
--
-- `seq = 103`, И ЭТО ПЕРВЫЙ НЕЗАНЯТЫЙ. Номер спрошен у РЕПОЗИТОРИЯ, а не у базы: часть
-- закоммиченных выпусков может быть не накатана, и `max(seq)` в базе врёт в меньшую сторону.
-- Лента идёт до 102 (0327_releases_department_special_equipment.sql).
--
-- ВЫКАТ ДВУХФАЗНЫЙ, и вторая фаза не в этой миграции. После раздачи новой статики поднимается пол
-- версии клиента — `deploy-auto --client-floor=6`: правка чека пересоздаёт строки целиком, и
-- старая вкладка, не знающая поля, обнулила бы артикулы всех строк правимого чека (ADR 0202,
-- решение 5). До подъёма пола это окно открыто.
--
-- ОБРАТИМОСТЬ: строка ленты, `DELETE FROM app_releases WHERE seq = 103`. Колонка при откате кода
-- остаётся: пустая лишняя колонка старому коду не мешает, а её снос потерял бы введённые артикулы.

INSERT INTO app_releases (seq, version, released_on, title, adrs, items) VALUES (
  103, '0.1.92.0202', '2026-09-21', 'Артикул в строке чека на автозапчасти', '{202}',
  '[
    {"kind":"feature","text":"У строки чека появился артикул — отдельной графой, как в счёте: его набирают в окне «Принять чек» и видят в карточке чека и в окне «Запчасти машины»"},
    {"kind":"feature","text":"Поиск по разделу находит чек по артикулу наравне с продавцом, номером и наименованием: деталь опознаётся даже тогда, когда продавец назвал её по-своему"},
    {"kind":"improvement","text":"Артикул необязателен: у бумаги без такой графы ячейка остаётся пустой и ничего не требует"}
  ]'::jsonb
);
