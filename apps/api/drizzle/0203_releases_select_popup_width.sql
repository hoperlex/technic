-- Запись журнала обновлений (ADR 0077): выпуск 44 — ширина раскрытых списков портала.
--
-- Схему миграция не трогает: она только заводит запись выпуска, как 0126, 0128, 0134, 0135, 0140,
-- 0158, 0159, 0160, 0163, 0165, 0168, 0169, 0170, 0173, 0174, 0184, 0189, 0190, 0193, 0196, 0200 и
-- 0202 (ADR 0077 §4). Работа целиком во фронте: правило ширины живёт в корневом `ConfigProvider`
-- (ADR 0136), сервер о нём не знает.
--
-- Номер файла и `seq` взяты по факту на момент создания: на 25.08.2026 последняя миграция в дереве
-- — 0202, последний выпуск — 51 (там же), отсюда файл 0203 и `seq = 52`. Разойдись эта пара с
-- очередью — накат упёрся бы в первичный ключ `app_releases`.
--
-- Версия — `0.1.44.0136`: выпуск 43 уже занят назначениями внутри срока заказа (`0.1.43.0126`,
-- миграция 0200), а у этой работы свой ADR, поэтому подверсией она не идёт.
--
-- Пункты писаны от лица того, кто выбирает площадку в фильтре: что изменилось на экране и где
-- проходит граница. Цена решения — отключённая виртуализация списка — в журнал не выносится: это
-- разговор ADR, а не человека, открывшего список.

INSERT INTO app_releases (seq, version, released_on, title, adrs, items) VALUES (
  52, '0.1.44.0136', '2026-08-25', 'Раскрытые списки перестали обрезать длинные названия', '{136}',
  '[
    {"kind":"improvement","text":"Раскрытый список больше не ограничен шириной поля: он расширяется под свои названия, поэтому площадки с адресом, типы техники и позиции справочников стало заметно легче различать. Раньше так вёл себя один фильтр площадки в вывозе мусора — теперь правило одно на весь портал"},
    {"kind":"improvement","text":"Совсем длинные значения список показывает до ширины окна, остаток — многоточием: за край экрана он не выходит ни на мониторе, ни на планшете"},
    {"kind":"fix","text":"На телефоне поведение прежнее: список во всю ширину поля, как и был"}
  ]'::jsonb
);
