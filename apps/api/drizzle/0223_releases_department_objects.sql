-- Запись журнала обновлений (ADR 0077): выпуск 62 — у отдела может быть несколько площадок.
--
-- Схему этого выпуска меняет соседняя миграция 0221 (таблица `department_construction_objects`,
-- перенос данных и триггер совместимости). Здесь только запись для людей.
--
-- Номер файла, `seq` и версия взяты ПО ФАКТУ в момент создания, а не зарезервированы планом: за
-- сутки три параллельные работы заняли ADR 0141–0143, миграции 0216–0220 и 0222, выпуски 59, 60 и
-- 61. На 28.08.2026 последней миграцией была 0222, последним выпуском — 61 (`0.1.52.0143`,
-- пожелания при регистрации), отсюда файл 0223 и `seq = 62`. Хвост версии — номер ADR решения
-- (ADR 0144), счётчик выпусков продолжает `0.1.53`.
--
-- ПЕРВЫЙ ПУНКТ ГОВОРИТ И О ТОМ, ЧЕГО РАНЬШЕ БЫЛО НЕЛЬЗЯ. «Площадка стала множественной» само по
-- себе ничего не объясняет тому, кто не знал об ограничении: поле в карточке отдела и раньше
-- выглядело обычным выбором из справочника. Поэтому сказано, зачем это понадобилось (гарантийный
-- отдел ведёт объекты, которые меняются) и что набор правят в одном месте.
--
-- ПРО ГАШЕНИЕ СЕССИЙ СКАЗАНО ОТДЕЛЬНОЙ ФРАЗОЙ, ХОТЯ ЭТО НЕ НОВОСТЬ: смена одной площадки закрывала
-- сессии и раньше (ADR 0062). Но теперь набор правят чаще и по одному объекту, и человек, снявший
-- одну площадку из пяти, увидит, что из портала выбросило весь отдел. Умолчать — значит получить
-- обращение «сломался вход».
--
-- ТРЕТИЙ ПУНКТ ПОМЕЧЕН `fix` И ГОВОРИТ ПРО БЕЗОПАСНОСТЬ ОБЫЧНЫМИ СЛОВАМИ. Дефект был в том, что
-- отзыв сессий при повторном использовании refresh-токена откатывался вместе с транзакцией, в
-- которой портал отвечал отказом: отказ человек видел, а сессии оставались живыми. Писать в журнал
-- «исправлен откат транзакции» бессмысленно — сказано то, что человек заметит: выбрасывает везде.
INSERT INTO app_releases (seq, version, released_on, title, adrs, items) VALUES (
  62, '0.1.53.0144', '2026-08-28', 'У отдела может быть несколько площадок', '{144}',
  '[
    {"kind":"improvement","text":"Отделу теперь задают не одну площадку, а сколько нужно: поле «Площадки» в карточке отдела стало множественным. Сотрудники такого отдела заказывают вывоз мусора с каждой из этих площадок наравне со штабом и видят в списке заявок только их. Раньше площадка была одна, и отдел, отвечающий за несколько объектов сразу, завести было нечем — ради этого всё и сделано. Набор правят в справочнике отделов, там же его видно целиком; смена набора закрывает открытые сессии всех сотрудников отдела — у них меняется то, что им видно, и портал попросит войти заново"},
    {"kind":"improvement","text":"Когда заявку на технику заводит отдел, портал предлагает адреса всех его площадок, а не одной. Что отдел может заказывать и какие заявки ему видны, при этом не изменилось"},
    {"kind":"fix","text":"Если refresh-токеном воспользовались повторно — так выглядит утечка, — портал теперь закрывает все сессии этой цепочки, а не только отвечает отказом. Раньше отказ приходил, но сами сессии оставались живыми, и вход по ним продолжал работать"}
  ]'::jsonb
);
