-- Запись журнала обновлений (ADR 0077): выпуск 80 — топливо в баке на концах смены
-- (ADR 0163, планы `docs/driver-fuel-readings-plan.md` и `docs/garage-fuel-readings-plan.md`).
--
-- Схему этот файл не трогает: две колонки и оба CHECK завела соседняя `0267`. Эта запись входит в
-- G вместе с API и гаражом; водительская форма включается только следующим полным релизом D.
--
-- ОТКУДА НОМЕРА. Перед созданием файла 04.09.2026 числовые имена сверены по всему каталогу, включая
-- незакоммиченные файлы соседних потоков: `0267` заняла схема этой волны, следующим свободным был
-- 0268.
--
-- `seq = 80`, И ЭТО ПЕРВЫЙ НЕЗАНЯТЫЙ. Номер спрошен у репозитория
-- (`grep -h "^  [0-9]\+, '0\.1\." apps/api/drizzle/*releases*.sql`): лента кончается на 79
-- (`0.1.68.0162`, меню действий заявки оргтехники), дыр перед ним нет.
--
-- ВЕРСИЯ `0.1.69.0163`: счётчик идёт дальше от 68, хвост — номер решения (0163).
--
-- ЧТО СКАЗАНО. Только то, что уже истинно в G: два уровня есть в общей модели и форме персонала,
-- запись можно дополнять по мере появления чисел, а гараж показывает их и отличает незавершённый
-- хвост смены. Текст не обещает водительскую форму, которой в G ещё нет.
--
-- ЧЕГО В ЗАПИСИ НЕТ И ПОЧЕМУ. Ни слова про договор провода (`optional()` без `default`), про
-- нормализатор черновика и про порядок выката — это устройство, а не то, что видно в портале. Нет и
-- обещания расхода: он вычислим с этого дня, но не считается (ADR 0163), и написать о нём в ленте
-- значило бы пообещать колонку, которой не будет. Зато сказано про пакетный приём: кнопка у
-- диспетчера изменит поведение сама, и узнать об этом из ленты честнее, чем по исчезнувшему отчёту.

INSERT INTO app_releases (seq, version, released_on, title, adrs, items) VALUES (
  80, '0.1.69.0163', '2026-09-04', 'Показания: топливо в баке на начало и конец смены', '{163}',
  '[
    {"kind":"feature","text":"В передаче показаний появились два новых числа: «Топливо на начало» и «Топливо на конец» смены. Оба необязательные — если уровень мерить нечем, поля остаются пустыми, как и раньше остаётся пустым одометр у техники без него. Поле «Заправлено» осталось прежним: это заправленное за смену, а не остаток в баке"},
    {"kind":"feature","text":"Одну запись показаний можно дополнять в течение дня, по мере появления чисел: утром — остаток на начало, днём — заправку по факту, вечером — счётчики и остаток на конец. Новые значения добавляются в ту же строку, дубля не появляется"},
    {"kind":"improvement","text":"В гараже три числа топлива видны везде, где видна отдельная смена: в карточке отчёта на приёме, в реестре приёма, в журнале машины и в журнальных выгрузках. В сводках за период их нет намеренно — остаток в баке нельзя складывать за месяц, складывается только заправленное"},
    {"kind":"improvement","text":"В реестре приёма появилась жёлтая пометка «вечерние показания не переданы»: она стоит у смены, по которой сдано только начало или только заправка. Отчёт с такой строкой не предлагается кнопкой пакетного приёма — но открывается и принимается вручную одним нажатием, если вечерних чисел у этой машины не будет"}
  ]'::jsonb
);
