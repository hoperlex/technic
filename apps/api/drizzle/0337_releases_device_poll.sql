-- Запись выпуска: опрос аппаратов по сети.
--
-- Решение — docs/adr/0205-device-network-poll.md. Схему меняет соседняя миграция
-- 0336_device_poll_attempts.sql (журнал попыток); эта — только лента выпусков.
--
-- `seq = 107`, И ЭТО ПЕРВЫЙ НЕЗАНЯТЫЙ. Номер спрошен у РЕПОЗИТОРИЯ, а не у базы: часть
-- закоммиченных выпусков может быть не накатана, и `max(seq)` в базе врёт в меньшую сторону.
-- Лента идёт до 106 (0335_releases_parse_rule_model.sql).
--
-- ОБРАТИМОСТЬ: строка ленты, `DELETE FROM app_releases WHERE seq = 107`.

INSERT INTO app_releases (seq, version, released_on, title, adrs, items) VALUES (
  107, '0.1.96.0205', '2026-09-22', 'Опрос аппаратов по сети', '{205}',
  '[
    {"kind":"feature","text":"В разделе «Техника» появилась вкладка «Опрос по сети»: карточка аппарата с адресом и кнопкой «Получить данные» — портал спрашивает счётчик у самого аппарата, не дожидаясь письма от него"},
    {"kind":"feature","text":"Снятый счётчик попадает в показания карточки рядом с присланными по почте — но только если серийный номер ответившего аппарата совпал с ожидаемым: по знакомому адресу после смены сетевых настроек отвечает другой принтер"},
    {"kind":"improvement","text":"Неудачный опрос объясняет себя словами и остаётся в журнале: молчание сети, отказ аппарата, отсутствие счётчика и незнакомая единица измерения — разные исходы с разными советами"}
  ]'::jsonb
);
