-- Запись выпуска: кнопка подтверждения сошедшегося талона, пофайловый значок «талон приложен» и
-- сохранённое состояние разбора (ADR 0155, миграция 0245, план `docs/waste-ticket-auto-confirm-plan.md`).
--
-- ОТКУДА НОМЕР. Проверено `ls apps/api/drizzle` непосредственно перед созданием файла (02.09.2026):
-- последняя миграция в дереве — `0245_waste_ticket_review_state.sql` (соседняя, схемная, того же
-- выката). Отсюда файл `0246`; хвост версии — номер решения (0155).
--
-- `seq = 74`, А НЕ 73. Первая редакция файла взяла 73 по `SELECT max(seq)` из dev-базы — и это
-- оказалось ловушкой общего дерева: 73 уже занят закоммиченным `0244_releases_auto_part_receipts.sql`
-- (`0.1.64.0154`), который на dev просто ещё не накатан. `max(seq)` отвечает про базу, а номер
-- выпуска раздаётся в РЕПОЗИТОРИИ, и спрашивать его надо там же — `grep seq apps/api/drizzle/*releases*`.
-- Потоки номеров миграций и ADR параллельны и заняты соседними сессиями: пока писались шесть
-- редакций плана, соседняя работа заняла и `0154` (автозапчасти), и четыре номера миграций.
--
-- ЗАПИСЬ ГОВОРИТ О ТОМ, ЧТО ЧЕЛОВЕК ЗАМЕТИТ, а заметит он три вещи: в строке списка вместо значков
-- появилась кнопка; значок «талон приложен» перестал дублировать соседние числа; фильтр «Требуют
-- разбора» начал показывать заявки, которые раньше не находились никак.
--
-- УЖЕСТОЧЕНИЕ НАЗВАНО ПРЯМО. Заявка с двумя приложенными талонами, где разобран один, теперь не
-- завершается, пока не разобран второй. Это следствие пофайлового счёта, и узнать о нём человек
-- должен из журнала выпусков, а не из отказа портала в конце дня.
--
-- ЧЕГО В ЗАПИСИ НЕТ: протокола блокировок, сохранённого состояния разбора с его ревизиями и того,
-- что кнопка не отбирает талоны в слепую перепроверку. Первые два человек не заметит вовсе, третье
-- касается только отчётов о качестве распознавания — про это сказано в ADR 0155 и в самом отчёте.
INSERT INTO app_releases (seq, version, released_on, title, adrs, items) VALUES (
  74, '0.1.64.0155', '2026-09-02', 'Вывоз мусора: подтверждение талона одной кнопкой', '{155}',
  '[
    {"kind":"feature","text":"У выполненной заявки, где талон распознан и сошёлся с закрытием, в списке вместо значков разбора появилась кнопка: один клик подтверждает талоны, не открывая карточку. Кнопки нет, если осталось хоть одно расхождение, предупреждение, нечитаемый файл, спорное поле или неразобранная бумага — такую заявку по-прежнему разбирают в карточке"},
    {"kind":"improvement","text":"Значок «талон приложен» больше не повторяет соседние числа. Раньше он горел, пока в заявке нет ни одного подтверждённого талона, — и строка показывала «ждут подтверждения: 2» и «приложено: 2» об одной и той же бумаге. Теперь он считается по каждому листу отдельно и горит только у той бумаги, до которой разбор не дошёл"},
    {"kind":"fix","text":"Фильтр «Требуют разбора» показывает заявки, где все талоны подтверждены, но расхождение не принято. Такая заявка не завершалась и при этом не находилась в списке — увидеть её можно было, только открыв карточку наугад"},
    {"kind":"improvement","text":"Заявка с двумя приложенными талонами, где разобран один, теперь не завершается, пока не разобран второй. Раньше её пропускал первый же подтверждённый талон, и вторая бумага оставалась незамеченной"}
  ]'::jsonb
);
