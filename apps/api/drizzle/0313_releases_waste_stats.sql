-- Запись журнала обновлений (ADR 0077, правила нумерации — ADR 0191): выпуск 95 — статистика
-- вывоза мусора за отчётный месяц (ADR 0193).
--
-- ОТКУДА НОМЕР ФАЙЛА. `ls apps/api/drizzle` непосредственно перед созданием (16.09.2026): последней
-- в дереве стоит `0312_releases_waybills_view.sql`, свободен `0313`.
--
-- `seq = 95`, И ЭТО ПЕРВЫЙ НЕЗАНЯТЫЙ. Номер спрошен у РЕПОЗИТОРИЯ
-- (`grep -h "^  [0-9]\+, '" apps/api/drizzle/*releases*.sql`), а не у базы: на technic_dev часть
-- закоммиченных выпусков не накатана, и `max(seq)` там врёт в меньшую сторону. Лента идёт до 94
-- (`0.1.83.0192`). Версия `0.1.84.0193`: счётчик выпусков линии `0.1` идёт дальше от 83, хвост —
-- номер последнего вошедшего решения.
--
-- ЧТО ЭТА ЗАПИСЬ ОБЕЩАЕТ. То, что человек увидит сразу после выката: во «Вывозе мусора» появилась
-- вкладка «Статистика» — площадки, объём и деньги за выбранный месяц, с детализацией по видам
-- отходов и подтверждёнными талонами кубами.
--
-- ЧЕГО НЕ ОБЕЩАЕТ. Ни выгрузки в Excel (её делает служебная книга аналитики, ADR 0182), ни
-- произвольного периода, ни металлолома с контейнерными операциями: вкладка отвечает про вывоз
-- мусора кубами за один месяц и ни про что другое. И она не открывается исполнителям вывоза —
-- у ролей от контрагента её нет.
--
-- ПОРЯДОК ВЫКАТА. Обычная пара сервер + клиент, схема не меняется: старый клиент новой ручки не
-- знает, новый без сервера показал бы пустую вкладку — поэтому сервер первым.
--
-- ОБРАТИМОСТЬ: строка ленты, `DELETE FROM app_releases WHERE seq = 95`.

INSERT INTO app_releases (seq, version, released_on, title, adrs, items) VALUES (
  95, '0.1.84.0193', '2026-09-16', 'Статистика вывоза мусора за отчётный месяц', '{193}',
  '[
    {"kind":"feature","text":"Во «Вывозе мусора» появилась вкладка «Статистика»: выбираете отчётный месяц и видите по каждой площадке вывезенный объём и стоимость"},
    {"kind":"feature","text":"Название площадки открывает детализацию по видам отходов: объём, стоимость, подтверждённые талонами кубы и их стоимость, внизу — строка «Итого»"},
    {"kind":"feature","text":"Объём и стоимость показывают и вывезенное, и заказанное ещё не закрытыми заявками месяца: доля, которая пока не вывезена, подписана под числом"},
    {"kind":"improvement","text":"Под таблицей видно, насколько числам можно верить: сколько вывозов без принятого талона и сколько заявок не удалось оценить"}
  ]'::jsonb
);
