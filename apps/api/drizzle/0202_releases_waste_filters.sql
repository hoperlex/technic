-- Запись журнала обновлений (ADR 0077): подверсия выпуска 42 — фильтры списка заявок на вывоз.
--
-- Схему миграция не трогает: она только заводит запись выпуска, как 0126, 0128, 0134, 0135, 0140,
-- 0158, 0159, 0160, 0163, 0165, 0168, 0169, 0170, 0173, 0174, 0184, 0189, 0190, 0193, 0196 и 0200
-- (ADR 0077 §4). Схемы работа не требует вовсе: фильтр по виду предмета спрашивает существующий
-- справочник `container_types`, период — существующую колонку `delivery_at`.
--
-- Номер файла и `seq` взяты по факту на момент создания: на 25.08.2026 последняя миграция в
-- дереве — 0201, последний выпуск — 50 (0200), отсюда файл 0202 и `seq = 51`. Разойдись эта пара
-- с очередью — накат упёрся бы в первичный ключ `app_releases`.
--
-- Версия — подверсия `0.1.42.1`, как выпуск 35 у гаража: своего ADR за работой нет, она доводит
-- список заявок на вывоз, заведённый выпуском 42 (`0.1.42.0135`), — отсюда и ссылка на ADR 0135.
-- Порядок в журнале задаёт `seq`, а не строка версии (ADR 0077 §3), и хвост подверсии его не
-- ломает.
--
-- Пункты писаны от лица того, кто открывает список утром: чем теперь искать площадку, где взять
-- нужную неделю и как спросить «все самосвалы». Устройство — отдельный параметр `containerKind`,
-- границы суток в поясе портала — в журнал не выносится: его читают не программисты.

INSERT INTO app_releases (seq, version, released_on, title, adrs, items) VALUES (
  51, '0.1.42.1', '2026-08-25', 'Вывоз мусора: фильтры знают адрес площадки, период и весь вид предмета', '{135}',
  '[
    {"kind":"improvement","text":"Фильтр по площадке называет её адресом: «С-12 — Корпус 3 — г. Москва, Ленина, 14». По адресу же работает и поиск в поле — площадку ищут именно так, а не по коду, который помнят единицы"},
    {"kind":"fix","text":"Список площадок раскрывается по своей ширине, а не по ширине поля: длинные названия с адресом больше не обрезаются многоточием на середине, по которому не отличить один корпус от другого"},
    {"kind":"feature","text":"В списке заявок появился период подачи — две даты, как в «Истории» и путевых листах. Любая граница необязательна: «с начала месяца» и «по пятницу» спрашивают не реже полного промежутка"},
    {"kind":"feature","text":"В фильтре «Контейнер / машина» первым пунктом каждой группы стоит весь её вид — «Все контейнеры» и «Все самосвалы». Вопрос «что возили самосвалами» перестал требовать перебора типов по одному, при котором заведённый вчера тип молча выпадал из ответа"},
    {"kind":"fix","text":"Период в «Истории» считает сутки по московскому времени: заявки последнего дня больше не теряются. Заметнее всего это было на телефоне — выбранный «по 20 августа» отсекал само 20-е"}
  ]'::jsonb
);
