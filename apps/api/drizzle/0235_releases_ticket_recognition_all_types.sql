-- Запись журнала обновлений (ADR 0077): выпуск 69 — талоны читаются у заявок всех типов.
--
-- Схему миграция не трогает: работа целиком в одном условии постановки задачи распознавания
-- (`enqueueTicketRecognition`, ADR 0150). Данные она тоже не трогает намеренно — прошлые закрытия
-- прочих типов не поднимаются в разбор ни задачей, ни обратной засыпкой: их бумага так и лежит
-- непрочитанной, и поднимают её поштучно кнопкой «Перераспознать» (ADR 0150, решение 3).
--
-- Номер файла и `seq` продолжают ряд: `0234` с `seq = 68` занят переоформлением листов ЭСМ-2 в тот
-- же день. Счётчик выпусков — `0.1.60`, хвост версии — номер ADR решения (ADR 0150).
--
-- ЗАПИСЬ ВЫПУСКА НУЖНА, ХОТЯ ЭКРАНЫ НЕ ИЗМЕНИЛИСЬ НИ В ОДНОЙ ТОЧКЕ. Меняется то, что человек
-- увидит завтра своими глазами: у заявок металлолома и контейнерных операций появляются
-- распознанные талоны, а их завершение начинает требовать подтверждения бумаги — как у вывоза
-- мусора. Умолчать об этом значило бы дать диспетчеру отказ завершения без объяснения, откуда он
-- взялся.
INSERT INTO app_releases (seq, version, released_on, title, adrs, items) VALUES (
  69, '0.1.60.0150', '2026-09-01', 'Талоны распознаются у заявок всех типов', '{150}',
  '[
    {"kind":"feature","text":"Портал читает талоны у заявок всех типов, а не только у вывоза мусора: металлолом и операции с контейнерами тоже попадают в разбор. Проверяются номер (повторно предъявленную бумагу теперь видно у любого перевозчика), день вывоза и адрес площадки. Сверка объёма у металлолома не считается — его принимают по весу, сравнивать не с чем"},
    {"kind":"improvement","text":"Заявки металлолома и операций с контейнерами, закрытые после этого обновления, завершаются только после подтверждения талона — так же, как заявки на вывоз мусора. Заявки, закрытые раньше, не изменились: их бумага осталась непрочитанной, поднять её можно кнопкой «Перераспознать» в карточке"},
    {"kind":"fix","text":"Кнопка «Перераспознать» перестала обещать несделанное: у заявок не про вывоз мусора она отвечала «Файл отправлен на повторное распознавание» и не отправляла ничего — ни результата, ни отказа увидеть было нельзя"}
  ]'::jsonb
);
