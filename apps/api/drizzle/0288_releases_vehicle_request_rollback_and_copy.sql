-- Запись журнала обновлений (ADR 0077): выпуск 85 — заказ ТС, две правки за один заход:
-- возврат в «Новую» больше не снимает визу (ADR 0172) и копия заявки из карточки (ADR 0173).
--
-- ОТКУДА НОМЕР. `ls apps/api/drizzle` перед созданием файла (08.09.2026): последней в дереве стоит
-- `0287_releases_mech_approval.sql` соседнего потока — она появилась, пока шла эта волна, и заняла
-- номер, свободный часом раньше. Свободен 0288, он и взят. Своих миграций у волны нет вовсе:
-- обе правки схемы не касаются.
--
-- `seq = 85`, И ЭТО ПЕРВЫЙ НЕЗАНЯТЫЙ. Номер спрошен у РЕПОЗИТОРИЯ
-- (`grep -h "^  [0-9]\+, '" apps/api/drizzle/*releases*.sql`), а не у базы: на technic_dev часть
-- закоммиченных выпусков просто не накатана, и `max(seq)` там врёт в меньшую сторону. Лента идёт
-- до 84 (`0.1.73.0171`, виза площадки в механизации). Версия `0.1.74.0173`: счётчик идёт дальше
-- от 73, хвост — номер последнего решения выпуска.
--
-- ПОЧЕМУ ОДНА ЗАПИСЬ НА ДВЕ ТЕМЫ. Выпуск — законченный блок работы (ADR 0077, решение 1), а не
-- отрезок календаря; здесь блок один: обе правки просил заказчик одной просьбой про «Заказ ТС»,
-- обе видны в одном и том же окне карточки заявки и обе доезжают одной сборкой портала.
--
-- ЧТО ЭТА ЗАПИСЬ ОБЕЩАЕТ И ЧЕГО НЕ ОБЕЩАЕТ. Обещано, что откат из-за поломки или отказа
-- исполнителя больше не останавливает работу до чужого рабочего дня, и что повторный заказ
-- набирается одним нажатием. Не обещано, что виза стала вечной: её по-прежнему снимают отзывом и
-- правкой заявки по существу — и не обещано, что копия унесёт вложения: файл живёт не более чем у
-- одной заявки.
--
-- ОБРАТИМОСТЬ: строка ленты, `DELETE FROM app_releases WHERE seq = 85`.

INSERT INTO app_releases (seq, version, released_on, title, adrs, items) VALUES (
  85, '0.1.74.0173', '2026-09-08', 'Заказ ТС: откат бережёт визу, заявку можно повторить копией', '{172,173}',
  '[
    {"kind":"fix","text":"Возврат заявки из «В работе» в «Новую» больше не снимает визу руководителя строительства. Откатывают обычно не заказ, а исполнение — машина не завелась, исполнитель отказался, — и согласие площадки этим не отменяется. Такую заявку диспетчер сразу отдаёт другой машине, не дожидаясь новой подписи. Всё остальное откат стирает как прежде: технику со ставками, факт, место в рейсе и перегоны"},
    {"kind":"improvement","text":"Снять визу по-прежнему есть чем: руководитель строительства отзывает её сам, а правка заявки по существу — срок, заказчик, техника — снимает её без спроса, как и у любой другой заявки. Переписанный после отката заказ согласовывают заново"},
    {"kind":"fix","text":"Окно возврата больше не обещает, что виза пропадёт: строки о ней в перечне стираемого нет, а в истории заявки не появляется запись «виза отозвана» о том, чего не было"},
    {"kind":"feature","text":"В карточке заявки в статусе «Новая» появилась кнопка «Создать копию»: она открывает форму заведения с составом этого же заказа — объект, тип и категория техники, контакт на месте, комментарий, а у грузоперевозки ещё и все ездки с адресами, грузом и контактами"},
    {"kind":"improvement","text":"Сроки копии сдвигаются вперёд сами: если прежние дни уже прошли, начало встаёт на первый доступный день, а длительность сохраняется — заказ на три дня остаётся заказом на три дня. Час подачи и часы ездок переезжают вместе с датой. Срок, который ещё впереди, остаётся как был"},
    {"kind":"improvement","text":"Копия честно показывает, чего в ней нет: вложения не переносятся (файл может принадлежать только одной заявке), и об этом сказано прямо в форме. Заказчик и позиция классификатора подставляются, только если они доступны копирующему и живы в справочнике, — иначе поле остаётся пустым и спрашивает"}
  ]'::jsonb
);
