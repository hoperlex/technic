-- Запись журнала обновлений (ADR 0077): выпуск 75 — ведение остатка автозапчастей закрыто
-- окончательно (ADR 0154, план `docs/auto-part-receipts-plan.md`, выкат 3 «Уборка»).
--
-- Схему этот файл не трогает: уборку прав сделала соседняя `0247`, здесь только строка ленты.
--
-- ОТКУДА НОМЕРА. Проверено `ls apps/api/drizzle` непосредственно перед созданием файла
-- (03.09.2026): последняя миграция в дереве — `0247_auto_parts_stock_grant_cleanup.sql`, соседняя,
-- того же выката. Отсюда файл `0248`.
--
-- `seq = 75` СПРОШЕН У РЕПОЗИТОРИЯ, а не у базы: `grep -h "^  [0-9]" apps/api/drizzle/*releases*.sql`
-- даёт последним выпуск 74 (`0.1.64.0155`, подтверждение талона одной кнопкой). `SELECT max(seq)`
-- на dev-базе ответил бы про базу, а номер выпуска раздаётся в дереве — на этой ловушке уже
-- спотыкалась `0246` и написала об этом в своей шапке.
--
-- ВЕРСИЯ `0.1.65.0154`: счётчик идёт дальше от 64, хвост — номер решения (0154). Хвост тот же, что
-- у записи выпуска 73 (`0.1.64.0154`): решение одно, а выкатов у него три, и различает записи
-- счётчик. Соседний по счётчику `0.1.64.0155` — чужая работа, занявшая номер между нашими
-- выкатами; уникальность `app_releases.version` от этого не страдает.
--
-- ПОЧЕМУ ЗАПИСЬ ПРИЕХАЛА ПОЗЖЕ САМОЙ ЗАМОРОЗКИ. Выпуск 2 «Заморозка» выкатывался без единой
-- миграции — это условие дешёвого отката (Р26), а запись выпуска такая же миграция, как всякая
-- другая. Поэтому лента рассказывает про заморозку на один выкат позже, чем та случилась. Цена
-- мала: склада пользователь не видит с выпуска 1, и узнаёт он об исчезновении из портала, а не из
-- ленты.
--
-- ЧТО СКАЗАНО: ровно то, что человек мог заметить сам, — остатка и строк запчастей в акте больше
-- нет нигде, и вернуть их из портала нельзя. Выпуск 1 обещал только исчезновение вкладки, и
-- договорить это надо прямо, а не оставить «раздел обновлён».
--
-- ЧЕГО В ЗАПИСИ НЕТ И ПОЧЕМУ: ни слова про снятые маршруты, про право `autoParts.stock`, про
-- уборку состава наборов и про архив снятого. Ручек этих человек не видел никогда, а набор,
-- потерявший состав, разбирают с администратором поимённо (§5 плана) — это разговор, а не строка
-- ленты выпусков.
INSERT INTO app_releases (seq, version, released_on, title, adrs, items) VALUES (
  75, '0.1.65.0154', '2026-09-03', 'Остаток автозапчастей больше не ведётся', '{154}',
  '[
    {"kind":"improvement","text":"Ведение остатка автозапчастей закрыто совсем. Вкладка склада с позициями, приходом-расходом и разметкой «к чему подходит» пропала из портала выпуском с чеками, а теперь этого учёта нет и на стороне сервера: завести позицию или движение остатка нельзя ничем. Ответ на вопрос «сколько вложено в эту машину» дают чеки во вкладке «Автозапчасти»"},
    {"kind":"improvement","text":"Строки запчастей в акте обслуживания убраны совсем: акт их больше не заводит, не показывает и не списывает со склада, а его аннулирование ничего никуда не возвращает. Купленное заносится чеком, и техника в строке чека говорит, на какую машину ушла деталь"}
  ]'::jsonb
);
