-- Запись выпуска: журнал отправки писем в портале.
--
-- Решение — docs/adr/0199-mail-log-tab.md. Подвкладка «Аудит» раздела «Рассылки»: очередь
-- `mail_messages` глазами администратора, с отбором по каналу и телом письма по клику.
--
-- `seq = 100`, И ЭТО ПЕРВЫЙ НЕЗАНЯТЫЙ. Номер спрошен у РЕПОЗИТОРИЯ, а не у базы: часть
-- закоммиченных выпусков может быть не накатана, и `max(seq)` в базе врёт в меньшую сторону. Лента
-- идёт до 99 (0321_releases_service_request_mail_events.sql).
--
-- НОМЕР ФАЙЛА — 0325, А НЕ 0324. Соседний поток (правила разбора писем от аппаратов) взял 0324 в
-- тот же час, и заметил я это на накате: у проекта уже есть две пары номеров, занятых дважды (0013
-- и 0081), и третью заводить незачем — журнал миграций различает файлы по имени, а человек читает
-- их по номеру.
--
-- ХВОСТ ВЕРСИИ — 0199: выпуск приносит своё решение, и это первый выпуск сотой линии счёта, где
-- хвост не повторяет предыдущий.
--
-- ОБРАТИМОСТЬ: строка ленты, `DELETE FROM app_releases WHERE seq = 100`.
--
-- ЧЕГО ВЫПУСК НЕ МЕНЯЕТ. Ни одного письма он не добавляет и не отменяет: портал шлёт ровно то же,
-- что и до него. Появляется только способ это увидеть — и индекс, которым список читается.

INSERT INTO app_releases (seq, version, released_on, title, adrs, items) VALUES (
  100, '0.1.89.0199', '2026-09-18', 'Видно, что портал отправил и чем это кончилось', '{199}',
  '[
    {"kind":"feature","text":"В разделе «Рассылки» появилась подвкладка «Аудит»: каждое письмо портала — когда составлено, по какой причине, кому и чем кончилась доставка"},
    {"kind":"feature","text":"Контуры отправки разделены переключателем: письма по заявкам оргтехники не смешиваются с заданиями водителям"},
    {"kind":"feature","text":"По клику на строке открывается само письмо — текстом и в том виде, в каком его получил адресат"},
    {"kind":"improvement","text":"Отказ почтового сервера виден прямо в строке: по нему сразу понятно, что письмо не дошло и почему"},
    {"kind":"improvement","text":"Журнал отбирается по причине письма, состоянию доставки, периоду, адресу и теме — по теме находятся все письма одной заявки"}
  ]'::jsonb
);
