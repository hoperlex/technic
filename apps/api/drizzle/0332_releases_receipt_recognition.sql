-- Запись выпуска: портал читает скан чека на автозапчасти.
--
-- Решение — docs/adr/0203-auto-part-receipt-recognition.md. Таблицы привезла миграция
-- 0331_receipt_scan_recognition.sql; здесь только лента выпусков.
--
-- `seq = 104`, И ЭТО ПЕРВЫЙ НЕЗАНЯТЫЙ. Номер спрошен у РЕПОЗИТОРИЯ, а не у базы: часть
-- закоммиченных выпусков может быть не накатана, и `max(seq)` в базе врёт в меньшую сторону.
-- Лента идёт до 103 (0330_releases_receipt_line_article.sql).
--
-- ВЫПУСК ПРИЕЗЖАЕТ ВЫКЛЮЧЕННЫМ. RECEIPT_OCR_ENABLED по умолчанию false: код выкатывается раньше
-- замера, как это было у талонов (ADR 0114). Включение — отдельное решение после того, как на
-- настоящих счетах измерена точность по каждому полю, и в первую очередь по артикулу: он самый
-- мелкий шрифт в таблице, и ошибка в один символ в нём делает значение хуже пустого.
--
-- ОБРАТИМОСТЬ: строка ленты, `DELETE FROM app_releases WHERE seq = 104`. Сам выпуск откатывается
-- выкатом прежнего кода: рубильник выключен, задач в очереди нет, таблицы пусты.

INSERT INTO app_releases (seq, version, released_on, title, adrs, items) VALUES (
  104, '0.1.93.0203', '2026-09-21', 'Портал читает скан чека на запчасти', '{203}',
  '[
    {"kind":"feature","text":"Скан чека, загруженный в окне «Принять чек», читается моделью: шапка и строки таблицы подставляются в форму по нажатию, машину в строке по-прежнему ставит человек"},
    {"kind":"feature","text":"Портал сверяет сумму подставленных строк с «Итого» на бумаге и предупреждает, если часть позиций осталась за кадром или скан уже подшит к другому чеку"},
    {"kind":"improvement","text":"Прочитанное, что не годится для чека — дробное количество, дата в будущем, слишком длинное наименование, — в поля не подставляется: портал показывает, что увидел, и оставляет решение человеку"},
    {"kind":"improvement","text":"Возможность включается после замера на настоящих счетах: пока распознавание выключено, окно работает как прежде"}
  ]'::jsonb
);
