-- Запись выпуска: состояние подсистемы чтения и замер счетов.
--
-- Решение — docs/adr/0203-auto-part-receipt-recognition.md (дополнено разделом «Последствия»).
-- Схему выпуск не меняет вовсе: ручка состояния считает по уже существующим таблицам.
--
-- `seq = 105`, И ЭТО ПЕРВЫЙ НЕЗАНЯТЫЙ. Номер спрошен у РЕПОЗИТОРИЯ, а не у базы: часть
-- закоммиченных выпусков может быть не накатана, и `max(seq)` в базе врёт в меньшую сторону.
-- Лента идёт до 104 (0332_releases_receipt_recognition.sql).
--
-- ОБРАТИМОСТЬ: строка ленты, `DELETE FROM app_releases WHERE seq = 105`.

INSERT INTO app_releases (seq, version, released_on, title, adrs, items) VALUES (
  105, '0.1.94.0203', '2026-09-22', 'Портал говорит, почему скан не прочитался', '{203}',
  '[
    {"kind":"improvement","text":"Когда скан не удалось прочитать, окно объясняет причину: сбой временный и попытки продолжатся сами или сервис не настроен и нужен администратор"},
    {"kind":"fix","text":"Баннер состояния распознавания талонов отдавал ошибку вместо ответа при отказе доступа к сервису — там, где он нужнее всего"}
  ]'::jsonb
);
