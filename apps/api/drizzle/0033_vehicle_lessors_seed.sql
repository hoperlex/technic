-- Первичное наполнение арендодателей ТС (ADR 0010): организации, у которых техника берётся
-- в аренду сегодня. Отдельный файл от 0032: значение enum `vehicle_lessor` добавлено там, а новое
-- значение нельзя использовать в транзакции, которая его добавила.
--
-- ИНН «ЭВЕРЕНТ» пришёл из учётных документов; у «Спецпорт «Надежда» и «ЯРД Империал» он взят из
-- ЕГРЮЛ по наименованию (оба — московские организации с основным ОКВЭД по аренде и грузовым
-- перевозкам, что совпадает с их ролью здесь). Все три прошли проверку контрольной суммы.
-- Наименования — как в ЕГРЮЛ, поэтому расходятся с бытовым написанием: сопоставление документов
-- идёт по нормализованной форме, а расхождения в написании заводятся синонимами.
--
-- Синонимы намеренно не заводятся: как эти организации пишут в накладных, пока неизвестно,
-- а неверный синоним «уводит» распознавание на чужого контрагента.
--
-- Идемпотентно по ИНН — ключу идентичности контрагента (уникальный индекс counterparties_inn_unique
-- из 0022 частичный, поэтому вывод индекса тоже с WHERE).
INSERT INTO counterparties (type, name, inn) VALUES
  ('vehicle_lessor', 'ООО «Спецпорт «Надежда»', '7726448317'),
  ('vehicle_lessor', 'ООО «ЭВЕРЕНТ»',           '7734432462'),
  ('vehicle_lessor', 'ООО «ЯРД Империал»',      '7710960488')
ON CONFLICT (inn) WHERE deleted_at IS NULL DO NOTHING;
