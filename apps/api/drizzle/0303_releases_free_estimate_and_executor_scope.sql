-- Запись журнала обновлений (ADR 0077): выпуск 91 — свободный объём работ и область исполнителя
-- (ADR 0179, план `docs/office-equipment-free-estimate-and-executor-scope-plan.md`, редакция 5,
-- выпуск A).
--
-- ОТКУДА НОМЕР. `ls apps/api/drizzle` непосредственно перед созданием файла (10.09.2026): последней
-- в дереве стоит `0302_releases_completion_actual_end_date.sql` соседнего потока, свободен `0303`.
-- Номера 0298–0300 заняты этой же волной (право «Ведению», след снятия исполнителя с индексом
-- автора, ключ рубильника), а 0301 и 0302 соседние потоки заняли ПОКА ВОЛНА ШЛА — за один день
-- дерево ушло вперёд на четыре номера, поэтому номер спрошен у файлов дважды: при планировании и
-- перед `git add`.
--
-- `seq = 91`, И ЭТО ПЕРВЫЙ НЕЗАНЯТЫЙ. Номер спрошен у РЕПОЗИТОРИЯ
-- (`grep -h "^  [0-9]\+, '" apps/api/drizzle/*releases*.sql`), а не у базы: на technic_dev часть
-- закоммиченных выпусков не накатана, и `max(seq)` там врёт в меньшую сторону. Лента идёт до 90
-- (`0.1.79.0178`, закрытие заказа фактической датой). Версия `0.1.80.0179`: счётчик идёт дальше от
-- 79, хвост — номер решения выпуска.
--
-- ЧТО ЭТА ЗАПИСЬ ОБЕЩАЕТ И ЧЕГО НЕ ОБЕЩАЕТ. Обещаны три перемены, которые человек увидит сразу
-- после выката: объём работ можно вписать словами и одной суммой, «Ведение» раскладывает такую
-- запись по графам, причина замены исполнителя перестала быть обязательной.
--
-- НЕ ОБЕЩАНО САМОЕ ГРОМКОЕ — «исполнитель видит только свои заявки», и это не забывчивость. Код
-- сужения приезжает этим же выпуском, но ЗАКРЫТЫМ: ключ `service_request_executor_scope` заведён
-- миграцией 0300 выключенным, и до `UPDATE` область остаётся сегодняшней (ADR 0179, порядок
-- выката). Лента обязана рассказывать про то, что человек уже видит; пообещай она сужение сегодня —
-- сисадмин пошёл бы проверять и увидел бы прежний список. Про область будет своя запись — в тот
-- выпуск, которым её включают.
--
-- ПОРЯДОК ВЫКАТА (ADR 0179): `0298` → `0299` → `0300` (ключ выключен) → эта запись → полное
-- переключение API → портал → после health и smoke `UPDATE feature_flags SET is_enabled = true
-- WHERE key = 'service_request_executor_scope'` с обновлением сессии на клиентах и smoke
-- СУЩЕСТВУЮЩЕЙ сессией. Аварийное выключение — обратный `UPDATE`: он возвращает прежнюю область
-- целиком, потому что карты сквозной области этот выпуск не трогает (их уборка — выпуск B).
--
-- ОБРАТИМОСТЬ: строка ленты, `DELETE FROM app_releases WHERE seq = 91`.

INSERT INTO app_releases (seq, version, released_on, title, adrs, items) VALUES (
  91, '0.1.80.0179', '2026-09-10', 'Объём работ можно вписать словами и одной суммой', '{179}',
  '[
    {"kind":"feature","text":"Объём работ теперь заполняется двумя полями: описание словами и стоимость заказа. Перечень позиций из письма подрядчика вставляется как есть — переносы строк сохраняются, длина больше не упирается в четверть страницы. Прежний ввод по графам никуда не делся: режим переключается в самом окне"},
    {"kind":"feature","text":"Свободную запись можно позже разложить по графам: у службы, ведущей заявки, появилась кнопка «Разложить по графам». Если объём работ уже согласован, раскладка выпускает новую редакцию и отправляет её на согласование заново — сумма и подпись не расходятся с тем, что в ней написано"},
    {"kind":"improvement","text":"Причина замены исполнителя перестала быть обязательной: перестановка людей больше не упирается в поле. Если причину не указали, в истории заявки на её месте стоит прочерк, а комментарий исполнителю остаётся комментарием и в причину не превращается"},
    {"kind":"fix","text":"Стоимость в свободном режиме требуется всегда: пустое поле больше не превращается в «ноль рублей» ни при сохранении черновика, ни при отправке на согласование. Работы без оплаты по-прежнему предъявляются кнопкой «Гарантийный ремонт без оплаты»"}
  ]'::jsonb
);
