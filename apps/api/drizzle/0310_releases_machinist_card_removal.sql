-- Запись журнала обновлений (ADR 0077): выпуск 93 — снятая карточка машиниста перестала
-- останавливать заявки (ADR 0190, план docs/machinist-card-removal-plan.md).
--
-- ОТКУДА НОМЕР ФАЙЛА. `ls apps/api/drizzle` непосредственно перед созданием (14.09.2026): последней
-- в дереве стоит `0309_service_request_estimate_basis_locks.sql` соседней волны, свободен `0310`.
--
-- `seq = 93`, И ЭТО ПЕРВЫЙ НЕЗАНЯТЫЙ. Номер спрошен у РЕПОЗИТОРИЯ
-- (`grep -h "^  [0-9]\+, '" apps/api/drizzle/*releases*.sql`), а не у базы: на technic_dev часть
-- закоммиченных выпусков не накатана, и `max(seq)` там врёт в меньшую сторону. Лента идёт до 92
-- (`0.1.81.0181`). Версия `0.1.82.0190`: счётчик идёт дальше от 81, хвост — номер решения выпуска.
--
-- ЧТО ЭТА ЗАПИСЬ ОБЕЩАЕТ. То, что человек увидит сразу после выката: удаление карточки водителя
-- показывает заказы, которые он ведёт машинистом; заявка с уже снятым машинистом продлевается и
-- закрывается; в форме выписки ЭСМ-2 за отработанную неделю снова виден человек, уволенный после
-- неё; недельная заявка предупреждает о снятом машинисте до визы.
--
-- ЧЕГО НЕ ОБЕЩАЕТ. Автоматической замены машиниста: портал показывает, где он стоит, а назначает
-- другого человек. И плашки в карточке заказа — она в этот выпуск не вошла.
--
-- ПОРЯДОК ВЫКАТА (ADR 0146, решение 7). Подтверждение удаления делает `DELETE` несовместимым со
-- старой вкладкой: она шлёт запрос без тела. Поэтому сначала пара сервер + клиент
-- (`CLIENT_CONTRACT = 5`), затем — после проверки, что раздаётся новая статика, —
-- `deploy-auto --client-floor=5`.
--
-- ОБРАТИМОСТЬ: строка ленты, `DELETE FROM app_releases WHERE seq = 93`.

INSERT INTO app_releases (seq, version, released_on, title, adrs, items) VALUES (
  93, '0.1.82.0190', '2026-09-14', 'Снятая карточка машиниста больше не останавливает заявки', '{190}',
  '[
    {"kind":"fix","text":"Заявка, у машиниста которой сняли карточку, снова продлевается, сокращается и закрывается: путевой лист выписывается на того, кто уже напечатан в бумаге заказа, с его фамилией и табельным номером"},
    {"kind":"feature","text":"Удаление карточки водителя показывает, что за ней стоит: заказы, где человек — машинист, сроки и сколько листов ЭСМ-2 на него ещё выпишется. Удаление проходит только с подтверждением этого перечня, а журнал запоминает номера заказов"},
    {"kind":"feature","text":"Лист ЭСМ-2 задним числом снова выписывается на человека, уволенного после отработанной недели: в отборе машинистов такие карточки видны с датой снятия. На недели, кончающиеся позже удаления, назначить его нельзя"},
    {"kind":"improvement","text":"Недельная заявка предупреждает до визы, если бумага заказа пойдёт на снятую карточку машиниста"}
  ]'::jsonb
);
