-- Запись журнала обновлений (ADR 0077, правила нумерации — ADR 0191): выпуск 97 — колонка
-- «Талоны» в вывозе мусора стала кнопкой разбора (ADR 0195).
--
-- ОТКУДА НОМЕР ФАЙЛА. `ls apps/api/drizzle` непосредственно перед созданием (17.09.2026): последней
-- в дереве стоит `0315_releases_fuel_norms.sql` прошлой волны, свободен `0316`.
--
-- `seq = 97`, И ЭТО ПЕРВЫЙ НЕЗАНЯТЫЙ. Номер спрошен у РЕПОЗИТОРИЯ
-- (`grep -h "^  [0-9]\+, '" apps/api/drizzle/*releases*.sql`), а не у базы: на technic_dev часть
-- закоммиченных выпусков не накатана, и `max(seq)` там врёт в меньшую сторону. Лента идёт до 96
-- (`0.1.85.0194`). Версия `0.1.86.0195`: счётчик выпусков линии `0.1` идёт дальше от 85, хвост —
-- номер последнего вошедшего решения.
--
-- ЧТО ЭТА ЗАПИСЬ ОБЕЩАЕТ. То, что человек увидит сразу после выката: в списке заявок на вывоз
-- колонка «Талоны» показывает не разбивку значков, а один крестик, и он открывает карточку прямо
-- на разборе; что именно за заявкой числится, написано в подсказке словами.
--
-- ЧЕГО НЕ ОБЕЩАЕТ. Ни одного нового числа и ни одной новой проверки: сверка считает ровно то же,
-- что считала. Кнопка подтверждения у сошедшейся заявки осталась на месте и работает как прежде.
--
-- ПОРЯДОК ВЫКАТА. Схему решение не трогает вовсе — выкат клиентский, одной парой с этой записью;
-- сервер менять не требуется.
--
-- ОБРАТИМОСТЬ: строка ленты, `DELETE FROM app_releases WHERE seq = 97`.

INSERT INTO app_releases (seq, version, released_on, title, adrs, items) VALUES (
  97, '0.1.86.0195', '2026-09-17', 'Колонка «Талоны» стала кнопкой разбора', '{195}',
  '[
    {"kind":"improvement","text":"В списке заявок на вывоз мусора колонка «Талоны» вместо разбивки значков показывает один крестик — он открывает карточку заявки сразу на разборе талонов"},
    {"kind":"improvement","text":"Что за заявкой числится — расхождения, предупреждения, очередь на подтверждение, нечитаемые файлы, неразобранная бумага — написано в подсказке крестика словами, с числами"},
    {"kind":"improvement","text":"Красным крестик горит только там, где цифры не сошлись и разбор нужен сейчас"},
    {"kind":"improvement","text":"Кнопка подтверждения у сошедшейся заявки и галочка «разобрано, расхождений нет» остались прежними"}
  ]'::jsonb
);
