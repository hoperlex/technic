-- Запись журнала обновлений (ADR 0077): выпуск 54 — портал запоминает, что исправляли в
-- распознанных талонах.
--
-- Схему миграция не трогает — только запись выпуска. Схему этого выпуска меняет соседняя 0206
-- (таблица `waste_ticket_field_events`).
--
-- Номер файла и `seq` взяты по факту: на 26.08.2026 последней миграцией была 0205, последним
-- выпуском — 53 (версия `0.1.42.2`), отсюда файл 0207 и `seq = 54`. Счётчик выпусков продолжает
-- `0.1.44`, хвост версии — номер ADR решения: работа описана ADR 0114, отсюда `0.1.45.0114`.
--
-- ПУНКТ ВСЕГО ОДИН, и он о том, чего пользователь НЕ УВИДИТ. Экрана у журнала нет, кнопок тоже:
-- он копит данные о том, как часто человеку приходится исправлять машину. Умолчать нельзя по двум
-- причинам. Первая: в журнале сохраняются прежние значения полей — в том числе адреса объектов, и
-- люди вправе знать, что портал их запоминает. Вторая: смысл записи в том, чтобы через полгода
-- было чем ответить на вопрос «а стоит ли платить за распознавание» — и ответ будет собран из
-- работы, которую делают сейчас.
INSERT INTO app_releases (seq, version, released_on, title, adrs, items) VALUES (
  54, '0.1.45.0114', '2026-08-26', 'Портал запоминает, что исправляли в распознанных талонах', '{114}',
  '[
    {"kind":"improvement","text":"Портал начал вести журнал разбора талонов: что прочитала машина, что человек исправил и на что именно. Экрана у него нет и на работу он никак не влияет — это данные для настройки: по ним видно, в каких полях распознавание ошибается чаще, стоит ли переходить на другую модель и окупается ли повторное чтение. Сохраняются прежние и новые значения полей, включая адрес объекта с бланка; кто именно исправил, как и раньше, видно в журнале действий"}
  ]'::jsonb
);
