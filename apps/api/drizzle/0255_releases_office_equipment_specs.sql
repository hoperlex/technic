-- Запись журнала обновлений (ADR 0077): выпуск 77 — цветность печати второй строкой в колонке
-- «Тип» (ADR 0158, план `docs/office-equipment-specs-plan.md`).
--
-- Схему этот файл не трогает: таблицы завела `0252`, значения по парку — `0254`, здесь только
-- строка ленты.
--
-- ОТКУДА НОМЕРА. `ls apps/api/drizzle` перед созданием файла (03.09.2026): последняя миграция в
-- дереве — соседняя `0254_office_equipment_print_color_seed.sql` этого же выката.
--
-- `seq = 77`, А НЕ 76 — И ЭТО НЕ ОПЕЧАТКА. Номер спрошен у репозитория
-- (`grep -h "^  [0-9]" apps/api/drizzle/*releases*.sql`), и ответ там сейчас противоречив: **75
-- занят дважды** — этой работой (`0248`, «Остаток автозапчастей больше не ведётся») и соседним
-- потоком (`0253_releases_mech_models.sql`, «Механизация: модель выбирают из справочника»).
-- Соседняя миграция ещё не накатана и на любой живой базе упадёт дублем `app_releases_pkey`; когда
-- её поправят, свободным для неё станет 76. Поэтому здесь взят следующий за ним, а не первый
-- незанятый: занять 76 значило бы отобрать номер у соседа второй раз подряд.
--
-- ВЕРСИЯ `0.1.66.0158`: счётчик идёт дальше от 65, хвост — номер решения (0158).
--
-- ЧТО СКАЗАНО: ровно то, что человек увидит сам, — под типом появилась пометка. И то, чего он сам
-- не увидит, но обязан знать, чтобы не счесть портал сломанным: «н/д» у одной модели стоит не
-- потому, что забыли, а потому, что данные противоречивы, и ведут цветность в справочнике моделей,
-- а не в карточке аппарата.
--
-- ЧЕГО В ЗАПИСИ НЕТ И ПОЧЕМУ: ни слова про четыре новые таблицы, про замки целостности и про
-- колонку в файле обмена — первые два человек не видит вовсе, а обмен справочниками ведут два
-- человека на весь портал, и им об этом сказано подсказкой в самом файле.
INSERT INTO app_releases (seq, version, released_on, title, adrs, items) VALUES (
  77, '0.1.66.0158', '2026-09-03', 'Оргтехника: под типом видно, цветной аппарат или чёрно-белый', '{158}',
  '[
    {"kind":"feature","text":"В справочнике оргтехники и во вкладке «Техника» под типом аппарата появилась пометка: «цв.» — печатает в цвете, «ч/б» — чёрно-белый, «н/д» — данных пока нет. В карточке аппарата то же самое написано словом. У техники, которая не печатает (мониторы, ноутбуки, ИБП), пометки нет вовсе — там этого вопроса не задают"},
    {"kind":"improvement","text":"Цветность ведётся у модели, а не у каждого аппарата: проставили её у «Ricoh Aficio MP C2011SP» — и она встала сразу у всех тридцати трёх таких машин. Выбирается она в окне «Модели аппаратов», там же, где модель заводят"},
    {"kind":"improvement","text":"Парк размечен целиком: из 39 моделей МФУ цветность известна у 38 — 45 аппаратов цветных, 307 чёрно-белых. У «Ricoh Aficio MP 2051AD» стоит «н/д»: под этим названием у Ricoh есть и цветная, и чёрно-белая машина, и какая стоит у нас — покажет наклейка. Поставьте значение в окне моделей, когда сверите"}
  ]'::jsonb
);
