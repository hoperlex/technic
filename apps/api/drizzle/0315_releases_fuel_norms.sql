-- Запись журнала обновлений (ADR 0077, правила нумерации — ADR 0191): выпуск 96 — нормы расхода
-- топлива и сверка с ними в гараже (ADR 0194).
--
-- ОТКУДА НОМЕР ФАЙЛА. `ls apps/api/drizzle` непосредственно перед созданием (16.09.2026): последней
-- в дереве стоит `0314_fuel_norms.sql` этой же волны, свободен `0315`.
--
-- `seq = 96`, И ЭТО ПЕРВЫЙ НЕЗАНЯТЫЙ. Номер спрошен у РЕПОЗИТОРИЯ
-- (`grep -h "^  [0-9]\+, '" apps/api/drizzle/*releases*.sql`), а не у базы: на technic_dev часть
-- закоммиченных выпусков не накатана, и `max(seq)` там врёт в меньшую сторону. Лента идёт до 95
-- (`0.1.84.0193`). Версия `0.1.85.0194`: счётчик выпусков линии `0.1` идёт дальше от 84, хвост —
-- номер последнего вошедшего решения.
--
-- ЧТО ЭТА ЗАПИСЬ ОБЕЩАЕТ. То, что человек увидит сразу после выката: в «Технике» появился
-- справочник норм расхода топлива, а в статистике гаража и в карточке машины рядом с расходом
-- встали норма и отклонение от неё.
--
-- ЧЕГО НЕ ОБЕЩАЕТ. Ни самих норм (таблица приказа заводится отдельным сидом, когда её пришлют), ни
-- расхода «на сто километров» — производных портал не печатает, ни сверки по водителю: норма
-- адресована машине. И ни одной цифры у техники, по которой не сдают остатки топлива в баке:
-- сверять там нечего, и портал говорит это словами, а не нулём.
--
-- ПОРЯДОК ВЫКАТА. Схема первой (0314 — три новые таблицы, ничего не переписывает), затем обычная
-- пара сервер + клиент: старый клиент новых ручек не знает, новый без сервера показал бы пустые
-- колонки — поэтому сервер первым.
--
-- ОБРАТИМОСТЬ: строка ленты, `DELETE FROM app_releases WHERE seq = 96`.

INSERT INTO app_releases (seq, version, released_on, title, adrs, items) VALUES (
  96, '0.1.85.0194', '2026-09-16', 'Нормы расхода топлива и сверка с ними в гараже', '{194}',
  '[
    {"kind":"feature","text":"В «Справочниках» → «Техника» появился справочник норм расхода топлива: кнопка «Нормы расхода» в шапке и действие в строке машины"},
    {"kind":"feature","text":"У нормы две ставки — зимняя и летняя — и дата начала действия: правка заводит новую версию, а отчёты прошлых периодов считаются прежней ставкой"},
    {"kind":"feature","text":"В статистике гаража рядом с расходом встали норма и отклонение от неё, а сверху — сколько машин сверялось, сколько вышло за допуск и на сколько литров"},
    {"kind":"feature","text":"Переключатель «Только превышения» сжимает сводку до нарушителей, и его можно отправить ссылкой"},
    {"kind":"feature","text":"В карточке машины появилась полоса «Сверка с нормой» и три колонки в таблице месяцев"},
    {"kind":"improvement","text":"Границы зимнего сезона и допуск в процентах настраиваются в том же окне: они одни на весь портал"},
    {"kind":"improvement","text":"Рядом с нормой написано, по скольким сменам она посчитана: смена без сданных остатков топлива в сверку не идёт"},
    {"kind":"improvement","text":"Нормы выгружаются и загружаются файлом на вкладке «Обмен справочниками»"}
  ]'::jsonb
);
