-- Запись выпуска: ключи опознания аппаратов и правила разбора переехали в интерфейс.
--
-- Решение — docs/adr/0200-device-mail-identity-ui.md, план —
-- docs/office-equipment-mail-identity-ui-plan.md. Сама работа сделана 18.09.2026: структура едет
-- миграцией 0324, права не меняются вовсе — всё закрыто существующим `officeEquipment.telemetry`.
--
-- `seq = 101`, И ЭТО ПЕРВЫЙ НЕЗАНЯТЫЙ. Номер спрошен у РЕПОЗИТОРИЯ, а не у базы: часть
-- закоммиченных выпусков может быть не накатана, и `max(seq)` в базе врёт в меньшую сторону.
-- Лента идёт до 100 (0325_releases_mail_log.sql).
--
-- ОБРАТИМОСТЬ: строка ленты, `DELETE FROM app_releases WHERE seq = 101`.
--
-- ВЫКАТ НИЧЕГО НЕ ВКЛЮЧАЕТ САМ: приём писем по-прежнему ждёт рубильника `device_mail_intake`, а
-- правил разбора в базе нет ни одного — письма читаются встроенными метками профилей, как и до
-- этой волны.

INSERT INTO app_releases (seq, version, released_on, title, adrs, items) VALUES (
  101, '0.1.90.0200', '2026-09-18', 'Портал узнаёт аппарат по ключу, а не по совпадению', '{200}',
  '[
    {"kind":"feature","text":"Ключ опознания заводится прямо в карточке аппарата — не дожидаясь письма: серийный номер, имя устройства или сетевое имя. Накопленные письма этого аппарата применяются сразу"},
    {"kind":"feature","text":"Появился реестр «Ключи аппаратов»: видно, чем опознаётся каждый аппарат, кто это решил и когда. Ошибочная привязка снимается с обязательной причиной и остаётся видимой"},
    {"kind":"feature","text":"Ключи на весь парк заводятся файлом — справочником «Ключи опознания аппаратов» в обычном обмене; применение к очереди осталось отдельной кнопкой с числом писем"},
    {"kind":"feature","text":"Правила разбора настраиваются в портале: если аппарат подписывает поля по-своему, серийник и счётчик достаются правилом, а не правкой кода и выкатом"},
    {"kind":"improvement","text":"Правило проверяется на живом письме до сохранения: портал показывает, что именно вынулось и какую карточку это опознало бы, ничего при этом не записывая"},
    {"kind":"improvement","text":"Снятие привязки объясняет последствие прямо в окне: новые письма опознаваться не будут, а уже записанные показания останутся в карточке"}
  ]'::jsonb
);
