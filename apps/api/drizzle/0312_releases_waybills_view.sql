-- Запись журнала обновлений (ADR 0077, правила нумерации — ADR 0191): выпуск 94 — путевые листы
-- площадке и отделу (ADR 0192).
--
-- ОТКУДА НОМЕР ФАЙЛА. `ls apps/api/drizzle` непосредственно перед созданием (15.09.2026): последней
-- в дереве стоит `0311_waybills_view_grant.sql` этой же волны, свободен `0312`.
--
-- `seq = 94`, И ЭТО ПЕРВЫЙ НЕЗАНЯТЫЙ. Номер спрошен у РЕПОЗИТОРИЯ
-- (`grep -h "^  [0-9]\+, '" apps/api/drizzle/*releases*.sql`), а не у базы: на technic_dev часть
-- закоммиченных выпусков не накатана, и `max(seq)` там врёт в меньшую сторону. Лента идёт до 93
-- (`0.1.82.0190`). Версия `0.1.83.0192`: счётчик выпусков в линии `0.1` идёт дальше от 82, хвост —
-- номер последнего вошедшего решения.
--
-- ЧТО ЭТА ЗАПИСЬ ОБЕЩАЕТ. То, что человек увидит сразу после выката и выдачи полномочия: раздел
-- «Путевые листы» у площадки и отдела, в нём — своя бумага, её печать и выгрузка.
--
-- ЧЕГО НЕ ОБЕЩАЕТ. Ни выписки, ни аннулирования, ни подшивки сканов: держатель набора бумагу
-- смотрит и печатает, а ведёт её диспетчерская. И раздел не появляется сам — набор выдаёт
-- администратор галочкой в окне учётки.
--
-- ПОРЯДОК ВЫКАТА. Обычная пара сервер + клиент: контракт клиента не меняется, старая вкладка
-- продолжает работать — у неё просто нет фильтра по водителю, которого держатель и так не получит.
-- Перед выдачей набора на проде — проверка адресации рассылок (ADR 0192, «Следствия»):
-- `SELECT * FROM mailing_schedule_permissions WHERE permission = 'waybills.read'`.
--
-- ОБРАТИМОСТЬ: строка ленты, `DELETE FROM app_releases WHERE seq = 94`.

INSERT INTO app_releases (seq, version, released_on, title, adrs, items) VALUES (
  94, '0.1.83.0192', '2026-09-15', 'Путевые листы стали видны площадке и отделу', '{192}',
  '[
    {"kind":"feature","text":"Площадка и отдел видят журнал путевых листов по своим заявкам: номер, машину, водителя, рейс и талоны. Лист, в котором есть хотя бы один свой талон, виден целиком"},
    {"kind":"feature","text":"Бумагу можно напечатать и выгрузить файлом, а к листу — открыть подшитые сканы. Выписка, аннулирование и подшивка документов остаются у диспетчерской"},
    {"kind":"feature","text":"Доступ выдаётся поимённо: полномочие «Путевые листы: просмотр и печать» ставится галочкой в окне учётки и так же снимается"},
    {"kind":"improvement","text":"Отдел видит бумагу и по заявкам своего отдела, и по заявкам площадок своего отдела"}
  ]'::jsonb
);
