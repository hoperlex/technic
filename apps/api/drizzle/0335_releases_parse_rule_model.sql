-- Запись выпуска: правило разбора пишется на модельный ряд, письмо для проверки выбирается списком.
--
-- Решение — docs/adr/0204-device-parse-rule-model.md. Схему меняет соседняя миграция
-- 0334_device_parse_rule_model.sql (колонка `when_model` и пересобранный ключ дубля); эта — только
-- лента выпусков.
--
-- `seq = 106`, И ЭТО ПЕРВЫЙ НЕЗАНЯТЫЙ. Номер спрошен у РЕПОЗИТОРИЯ, а не у базы: часть
-- закоммиченных выпусков может быть не накатана, и `max(seq)` в базе врёт в меньшую сторону.
-- Лента идёт до 105 (0333_releases_recognition_health.sql).
--
-- ОБРАТИМОСТЬ: строка ленты, `DELETE FROM app_releases WHERE seq = 106`.

INSERT INTO app_releases (seq, version, released_on, title, adrs, items) VALUES (
  106, '0.1.95.0204', '2026-09-22', 'Правило разбора писем пишется на модельный ряд', '{204}',
  '[
    {"kind":"feature","text":"У правила разбора писем от аппаратов появилось условие по модели: одно правило закрывает весь модельный ряд вместо правила на каждый аппарат"},
    {"kind":"improvement","text":"Письмо для проверки правила выбирается из списка последних писем — раньше его идентификатор нужно было вписывать руками, а взять его было негде"},
    {"kind":"improvement","text":"Проверка правила объясняет отказ поимённо: какое условие не подошло, какая у письма модель и откуда она взята"}
  ]'::jsonb
);
