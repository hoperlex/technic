-- Запись журнала обновлений (ADR 0077): выпуск 59 — путевой лист ЭСМ-2 не бывает на два месяца.
--
-- Схему миграция не трогает вовсе: правило разреза живёт в расчёте, и `CHECK` о непересечении
-- месяца заводить отказались осознанно (ADR 0142, решение 6) — старые листы его нарушают, и
-- ограничение пришлось бы вешать `NOT VALID`, то есть надписью, которая ничего не проверяет.
--
-- Номер файла и `seq` взяты по факту: на 28.08.2026 в дереве заняты миграции по 0219 включительно
-- (параллельный поток — чат заявки оргтехники `0216` и пожелания при регистрации `0218`/`0219`),
-- свободным остался `0217`; последним выпуском был 58 (версия `0.1.49.0140`), отсюда `seq = 59`.
-- Счётчик выпусков продолжает `0.1.50`, хвост версии — номер ADR решения (ADR 0142; `0141` тем же
-- параллельным потоком занят под чат заявки).
--
-- ПУНКТ ПРО ПЕРЕОФОРМЛЕНИЕ ВЫПИСАННЫХ ЛИСТОВ СТОИТ ПЕРВЫМ И ПОМЕЧЕН `fix`. Человек, открывший
-- журнал путевых, увидит там не новые листы рядом со старыми, а **другие номера**: бланки на
-- 31.08–06.09 аннулированы и заменены парами. Умолчать об этом нельзя — исчезнувший номер строгой
-- отчётности читается как поломка учёта, а не как исправление.
--
-- ПУНКТ ПРО ЧИСЛА В БЛАНКЕ КАСАЕТСЯ И ТЕХ, У КОГО МЕСЯЦ НИ ПРИ ЧЁМ. Раньше в листе печатались все
-- семь чисел недели, даже если лист покрывает три дня; теперь — только свои. Заказчик, привыкший
-- проставлять часы напротив любой строки, обязан узнать, что пустая строка теперь означает «эти
-- дни не в этом листе».
INSERT INTO app_releases (seq, version, released_on, title, adrs, items) VALUES (
  59, '0.1.50.0142', '2026-08-28', 'Путевой лист ЭСМ-2 не бывает на два месяца', '{142}',
  '[
    {"kind":"fix","text":"Путевой лист ЭСМ-2 больше не выписывается сразу на два месяца. Неделя, в середине которой кончается месяц, закрывается двумя бланками: например, «31 — 31 августа» и «1 — 6 сентября». Уже выписанные листы на 31.08 — 06.09 аннулированы и заменены парами новых номеров — в журнале видно, какой номер что заменил. Отработанные листы прошлых недель не тронуты: их переоформляют только коррекцией, с правом и причиной"},
    {"kind":"improvement","text":"В бланке ЭСМ-2 теперь печатаются только числа своего периода. Раньше в семи строках недели стояли все семь чисел, даже если лист покрывает три дня, — и два листа одной недели выглядели бы одинаково. Теперь у августовского листа за 31-е напечатано одно число, у сентябрьского — шесть, и часы попадают в тот бланк, к которому относятся"},
    {"kind":"improvement","text":"У линейной техники, где лист ЭСМ-2 выписывают по требованию, просьба «выписать за неделю» на переходной неделе выдаёт сразу два листа — просить второй отдельно не нужно. В окне выписки заранее написано, какие периоды попадут в бланки"},
    {"kind":"improvement","text":"В недельной заявке в столбце ЭСМ-2 у такой недели показаны оба номера, и печать открывается для каждого из них"}
  ]'::jsonb
);
