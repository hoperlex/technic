-- Запись журнала обновлений (ADR 0077): выпуск 92 — менеджер заводит заявки оргтехники ролью
-- (ADR 0181, решение заказчика 10.09.2026).
--
-- ОТКУДА НОМЕР ФАЙЛА. `ls apps/api/drizzle` непосредственно перед созданием (10.09.2026): последней
-- в дереве стоит `0304_office_equipment_requester_drop_manager.sql` этой же волны, свободен `0305`.
--
-- `seq = 92`, И ЭТО ПЕРВЫЙ НЕЗАНЯТЫЙ. Номер спрошен у РЕПОЗИТОРИЯ
-- (`grep -h "^  [0-9]\+, '" apps/api/drizzle/*releases*.sql`), а не у базы: на technic_dev часть
-- закоммиченных выпусков не накатана, и `max(seq)` там врёт в меньшую сторону. Лента идёт до 91
-- (`0.1.80.0179`, свободный объём работ). Версия `0.1.81.0181`: счётчик идёт дальше от 80, хвост —
-- номер решения выпуска.
--
-- ЧТО ЭТА ЗАПИСЬ ОБЕЩАЕТ. Ровно то, что человек увидит сразу после выката: у менеджера в разделе
-- «Орг.техника» появились «Заявки» и «Гарантии», заявку он заводит на любую площадку и любой отдел,
-- а ведёт — свою.
--
-- ЧЕГО НЕ ОБЕЩАЕТ. Ни хода заявки (назначение, согласование объёма работ, приёмка — по-прежнему у
-- «Ведения», и выдаётся оно набором), ни денег: без права `serviceRequests.finance` и без
-- назначения карточка приходит без финансовой части. Про снятый у менеджеров набор «Оргтехника:
-- заявитель» лента молчит намеренно — доступ от этого не изменился ни на одно право, а строка
-- «у вас забрали полномочие» напугала бы держателя ровно там, где ничего не произошло.
--
-- ПОРЯДОК ВЫКАТА (ADR 0181): код (матрица роли + страж авторства) → `0304` (каталог и снятие пустых
-- выдач) → эта запись. В обратном порядке между накатом и выкатом менеджеры остались бы без модуля
-- заявок целиком: набор им уже не действует, а роль ещё не даёт.
--
-- ОБРАТИМОСТЬ: строка ленты, `DELETE FROM app_releases WHERE seq = 92`.

INSERT INTO app_releases (seq, version, released_on, title, adrs, items) VALUES (
  92, '0.1.81.0181', '2026-09-10', 'Менеджер заводит заявки на обслуживание оргтехники', '{181}',
  '[
    {"kind":"feature","text":"У менеджера в разделе «Орг.техника» открылись «Заявки» и «Гарантии». Заявку он заводит по любому аппарату компании — площадка и отдел его не делят, — а если нужной техники нет в справочнике, сообщает о ней прямо из формы"},
    {"kind":"improvement","text":"Полномочие «Оргтехника: заявитель» менеджерам больше не выдаётся и не требуется: те же права приходят самой должностью. У диспетчера этот доступ по-прежнему выдаётся полномочием — поимённо"},
    {"kind":"improvement","text":"Чужие заявки менеджер видит, но не правит: изменить, удалить и подшить бумаги можно к той заявке, которую он завёл сам. Вести чужие по-прежнему может тот, кому выдано полномочие «Оргтехника: ведение»"}
  ]'::jsonb
);
