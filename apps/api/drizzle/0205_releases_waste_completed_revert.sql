-- Запись журнала обновлений (ADR 0077): подверсия выпуска 42 — заявки с неразобранной бумагой
-- вернулись из «Завершена» в «Выполнена» (ADR 0135).
--
-- Схему миграция не трогает: она только заводит запись выпуска, как 0126, 0128, 0134, 0135, 0140,
-- 0158, 0159, 0160, 0163, 0165, 0168, 0169, 0170, 0173, 0174, 0184, 0189, 0190, 0193, 0196, 0200
-- и 0202 (ADR 0077 §4). Данные правит соседняя 0204 — своим файлом, потому что накат каждого
-- файла идёт своей транзакцией и запись выпуска не должна делить её с правкой заявок.
--
-- Номер файла и `seq` взяты по факту на момент создания: на 26.08.2026 последняя миграция в
-- дереве — 0204, последний выпуск — 52 (0203, версия `0.1.44.0136`), отсюда файл 0205 и
-- `seq = 53`. Разойдись эта пара с очередью — накат упёрся бы в первичный ключ `app_releases`.
--
-- Версия — подверсия `0.1.42.2`, вслед за `0.1.42.1` у фильтров вывоза: своего ADR за работой нет,
-- она поправляет выпуск 42 (`0.1.42.0135`) — отсюда и ссылка на ADR 0135. Порядок в журнале задаёт
-- `seq`, а не строка версии (ADR 0077 §3).
--
-- Пункты писаны от лица того, кто откроет портал утром и не найдёт вчерашних заявок в «Истории»:
-- какие вернулись и почему, что теперь с талонами, какие остались завершёнными и что делать, если
-- заявка и правда принята. Устройство — отбор по последнему переходу без автора, подтверждённый
-- талон как признак разбора — в журнал не выносится: его читают не программисты.

INSERT INTO app_releases (seq, version, released_on, title, adrs, items) VALUES (
  53, '0.1.42.2', '2026-08-26', 'Вывоз мусора: заявки с неразобранными талонами вернулись в работу', '{135}',
  '[
    {"kind":"fix","text":"Заявки, к которым талон приложен, но ни один талон не разобран, вернулись из «Истории» в «Выполнена». Прошлым выпуском портал завершил их сам — и это было неверно: бумага лежала нетронутой, а заявка числилась принятой"},
    {"kind":"improvement","text":"Талоны в таких заявках снова открыты: их можно править, подтверждать и разбирать, как в любой выполненной заявке. У завершённой заявки разбор закрыт, и до возврата вся эта бумага была заперта"},
    {"kind":"improvement","text":"Заявки, к которым талон не приложен вовсе, остались завершёнными: разбирать в них нечего, и возвращать их в рабочий список незачем"},
    {"kind":"improvement","text":"В истории заявки видно и перевод, и возврат — обе строки подписаны «Портал»: ничего не стёрто, порядок событий читается целиком"},
    {"kind":"improvement","text":"Заявку, по которой бумага действительно принята, завершайте из меню статуса — пункт «Завершена» на месте и работает как прежде. В «Историю» она уйдёт уже с вашим именем в журнале"}
  ]'::jsonb
);
