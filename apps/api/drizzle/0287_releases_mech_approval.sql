-- Запись журнала обновлений (ADR 0077): выпуск 84 — виза ответственного лица площадки в модуле
-- «Механизация» (ADR 0171, план `docs/mechanization-approval-and-grants-plan.md`, этап Э1).
--
-- ОТКУДА НОМЕР ФАЙЛА. `ls apps/api/drizzle` перед созданием (08.09.2026): 0284 и 0285 — свои
-- (схема визы и её состав в наборах), 0286 занял соседний поток (отбор срочных заявок оргтехники).
-- Свободен 0287, он и взят.
--
-- `seq = 84`, И ЭТО ПЕРВЫЙ НЕЗАНЯТЫЙ. Номер спрошен у репозитория
-- (`grep -h "^  [0-9]" apps/api/drizzle/*releases*.sql`), а не у базы: часть закоммиченных
-- выпусков на дев-базе просто не накатана, и `max(seq)` там врёт в меньшую сторону. Последняя
-- строка ленты — 83 (`0.1.72.0166`), отсюда версия `0.1.73.0171`: счётчик идёт дальше, хвост —
-- номер решения.
--
-- ЧТО ЭТА ЗАПИСЬ ОБЕЩАЕТ И ЧЕГО НЕ ОБЕЩАЕТ. Обещана подпись площадки перед входом аренды в работу
-- и то, что подписывающий видит свои заявки списком. НЕ обещаны письма («ждёт вашей визы»): у
-- модуля нет почтового контура вовсе, и уведомления — отдельная работа. Не обещано и то, что
-- назначаемым стал доступ к самому модулю: это следующий этап плана, и лента скажет о нём своей
-- строкой.
--
-- ПРО СТАРЫЕ ЗАЯВКИ — прямо, потому что человек это увидит: у аренд, взятых в работу до выката,
-- подписи нет и не появится. Прошлое не переписывается, а найти такие заявки можно отбором
-- «Виза: ждёт визы» в журнале закрытых.
--
-- ОБРАТИМОСТЬ: строка ленты, `DELETE FROM app_releases WHERE seq = 84`.

INSERT INTO app_releases (seq, version, released_on, title, adrs, items) VALUES (
  84, '0.1.73.0171', '2026-09-08', 'Механизация: аренду берут в работу после визы площадки', '{171}',
  '[
    {"kind":"feature","text":"У заявки на механизацию появилась виза ответственного лица площадки. Пока подписи нет, заявку нельзя взять в работу: у неё стоит тег «ждёт визы», а в меню строки и в карточке — кнопка «Согласовать». Подписывает руководитель строительства своей площадки, а заявку, заведённую от лица отдела, — ещё и руководитель этого отдела: кто первый, того подпись и стоит"},
    {"kind":"feature","text":"Свою собственную заявку визирующий подписывает самим фактом заведения — отдельно нажимать «Согласовать» не нужно. У администратора это правило не работает: он заводит заявку за того, кто до портала не добрался, и согласования этим не происходит"},
    {"kind":"feature","text":"В списке заявок и в журнале закрытых появился отбор «Виза»: «есть» и «ждёт визы». Им начинают день на площадке и им же офис смотрит, что подписано и можно брать в работу"},
    {"kind":"improvement","text":"Виза снимается сама, если заявку переписали по существу — сменили модель, срок, площадку или заявителя — и сделал это не тот, кто её подписывал. Комментарий, вложения и телефон встречающего подпись не трогают: согласовывают технику, срок и место"},
    {"kind":"improvement","text":"Кто и когда завизировал, видно в карточке заявки и в её истории — вместе со снятием визы и его причиной. Возврат заявки в «Новую» подпись сохраняет: он стирает договорённость с арендодателем, но согласия площадки не отменяет"},
    {"kind":"fix","text":"У аренд, взятых в работу до этого выпуска, визы нет и не появится: задним числом подписать их некому. Найти такие заявки можно тем же отбором «Виза» в журнале закрытых"}
  ]'::jsonb
);
