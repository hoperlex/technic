-- Запись журнала обновлений (ADR 0077): выпуск 79 — меню действий заявки оргтехники стало короче,
-- а перемещение техники записывают из карточки (ADR 0162, план
-- `docs/office-equipment-request-actions-menu-plan.md`, подзадача П1 и волны Э2, Э4).
--
-- Схему этот файл не трогает: вся волна — портал. Ни права, ни маршруты не менялись; серверная
-- ручка повтора письма (`POST /:id/notify`) намеренно оставлена до следующего выпуска, когда
-- поднимется пол клиента, — снимать её вместе с кнопкой значило бы ронять старый бандл в чужом
-- браузере на 404.
--
-- ОТКУДА НОМЕР. `ls apps/api/drizzle` перед созданием файла (04.09.2026): последняя миграция в
-- дереве — `0262_office_equipment_executor_grant.sql` соседнего потока (наборы профилей модуля).
-- Свободен 0263, он и взят.
--
-- `seq = 79`, И ЭТО ПЕРВЫЙ НЕЗАНЯТЫЙ. Номер спрошен у репозитория
-- (`grep -h "^  [0-9]" apps/api/drizzle/*releases*.sql`): 1…78 заняты ровно по одному разу, дыр и
-- повторов в ленте нет. Версия `0.1.68.0162`: счётчик идёт дальше от 67 (последняя строка ленты —
-- 78, `0.1.67.0160`), хвост — номер решения.
--
-- ПОЧЕМУ ОДНА ЗАПИСЬ, А НЕ ТРИ. План ведёт три волны раздельно (выключенный пункт, повтор письма,
-- перемещение) и по каждой обещает свою строку ленты — на случай раздельных выкатов. Выкатываются
-- они одним бандлом портала, и три записи об одном обновлении заставляли бы читателя искать
-- разницу, которой он не увидит. Разъедутся выкаты — разъедутся и записи; пока лента говорит о
-- том, что человек откроет и увидит.
--
-- ЧЕГО В ЗАПИСИ НЕТ И ПОЧЕМУ: ни слова про наборы скрытых ключей, общий перевод пунктов и
-- вычеркнутые из меню карточки кнопки согласования — это устройство, а не то, что видно в портале.
-- Про снятие «Закрыть работы» и откатов тоже ничего: они остаются на месте до приёмки быстрой
-- смены статуса (ADR 0161), и обещать их сегодня значило бы обещать чужой выпуск.
--
-- ОБРАТИМОСТЬ: строка ленты, `DELETE FROM app_releases WHERE seq = 79`. Сам выпуск обратим откатом
-- бандла — сервер эта волна не меняла.

INSERT INTO app_releases (seq, version, released_on, title, adrs, items) VALUES (
  79, '0.1.68.0162', '2026-09-04', 'Оргтехника: в меню заявки меньше лишнего', '{162}',
  '[
    {"kind":"improvement","text":"Из меню «Действия» заявки на обслуживание пропал пункт «Отправить письмо службе ещё раз». Письма по заявке уходят сами — при заведении и при отмене, — и отправлять их повторно вручную больше не нужно"},
    {"kind":"improvement","text":"Перемещение техники теперь записывают из карточки заявки: в строке «Какой аппарат» появилась ссылка «Записать перемещение». Из общего меню действие убрано — переезд аппарата меняет справочник, и делать это, не открыв заявку и не посмотрев реквизиты, было слишком легко. Кому действие было доступно, тому и осталось"},
    {"kind":"improvement","text":"В карточке заявки меню «Действия» больше не повторяет то, что и так есть на виду: «Согласовать объём работ» и «Не согласовать» остались кнопками под таблицей на вкладке «Объём работ» — там, где видно сумму, под которой подписываются"},
    {"kind":"fix","text":"Недоступное действие теперь выглядит недоступным везде одинаково. Раньше «Закрыть работы» у заявки без акта, счёта или гарантийного талона в списке было серым и объясняло причину, а из карточки нажималось, открывало окно — и упиралось в отказ. Теперь пункт неактивен в обоих местах и в обоих называет причину"}
  ]'::jsonb
);
