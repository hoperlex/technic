import { defineConfig } from 'vitest/config';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  // Те же алиасы слоёв, что у приложения, и из того же tsconfig.json: тест, собранный по другим
  // путям, проверял бы не тот код, который уедет в сборку.
  resolve: { tsconfigPaths: true },
  test: {
    include: ['test/**/*.test.{ts,tsx}'],
    // Компоненты проверяются рендером: поведение выпадающих списков видно только в DOM.
    environment: 'jsdom',
    /*
     * Пяти секунд по умолчанию хватает юнит-тесту, но не сценарному: тот рендерит страницу целиком
     * — таблица antd, форма, модальные окна — и ждёт ответов сети. При параллельном прогоне на
     * занятой машине такие тесты начинали падать по таймауту, причём каждый раз разные, и падение
     * читалось как поломка экрана. Отдельные файлы уже поднимали таймаут у себя: признак того, что
     * лечить надо здесь.
     */
    testTimeout: 20_000,
    setupFiles: ['test/setup.ts'],
  },
});
