/**
 * Служебное меню каркаса: руководства (`docs/manuals-plan.md`), техподдержка и журнал обновлений
 * (ADR 0077) — пункты, разбор нажатия и окна за ними.
 *
 * Виджет, а не фича: он собирает вместе три разных сценария и все их окна, а слайсу `features`
 * сосед по слою не виден. Каркасу отдаётся модель (пункты и нажатие) и подвал панели: меню
 * учётной записи на телефоне (ADR 0030) собирает сам каркас — служебные пункты стоят там в одном
 * списке со «Сменить пароль» и «Выйти», отдельным блоком их туда не вставить.
 */
export {
  useUtilityMenu,
  type UtilityMenuItem,
  type UtilityMenuModel,
} from './model/useUtilityMenu';
export { UtilityMenu } from './ui/UtilityMenu';
