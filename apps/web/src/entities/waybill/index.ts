/**
 * Путевой лист: бумага, с которой машина уходит в рейс, и строка журнала учёта (ADR 0037).
 * Снаружи берут `@entities/waybill` — внутренние модули слайса не видны, и перестроить его можно,
 * не трогая потребителей.
 *
 * `waybillKeys` теперь здесь, со всеми семействами журнала, — но такой же ключ пока стоит и в
 * `entities/vehicle-request`, откуда его берут окна и команды заказа техники. Двойственность
 * временная и снимается не отсюда: убрать тот ключ сейчас значит погасить его потребителей разом,
 * а оставить у себя реэкспорт — завести импорт соседа по слою, который границы запрещают. Он
 * уезжает одной правкой вместе с ними; чем двойственность безопасна и чем опасна, сказано у
 * самого ключа.
 *
 * `WaybillPrint` план этапа 2 кладёт сюда же (docs/frontend-fsd-stage-2.md §2.1), но компонент
 * остался в `components/`: сообщение об ошибке он берёт из `utils/format`, а legacy из `entities`
 * запрещён границами. Рядом остался и `WaybillFiles` — у него барьера два: тот же `utils/format`
 * и вложения, которые он собирает из `filesApi` со списком ссылок `FileLinks`, то есть в слайсе
 * стал бы импортом соседа `waybill` → `file`. Оба переедут вслед за тем, на чём стоят.
 */
export { waybillKeys } from './api/keys';
export { waybillsApi } from './api/waybillsApi';
