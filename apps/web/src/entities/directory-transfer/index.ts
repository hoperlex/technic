/**
 * Обмен справочниками через файл Excel (ADR 0073): выгрузка книгой, правка её человеком и загрузка
 * обратно с предварительным отчётом. Снаружи берут `@entities/directory-transfer` — внутренние
 * модули слайса не видны, и перестроить его можно, не трогая потребителей.
 *
 * Слайс на обмен, а не на справочники: ручки общие на все восемнадцать ресурсов сразу, какой
 * именно выгружается — задаёт ключ в адресе. Разложи их по слайсам самих справочников, и одно
 * правило обмена разошлось бы на восемнадцать мест, где оно однажды описано по-разному.
 */
export { directoriesApi } from './api/directoriesApi';
export { directoryTransferKeys } from './api/keys';
