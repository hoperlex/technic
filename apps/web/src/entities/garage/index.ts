/**
 * Гараж: чем заняты собственная техника и водители в конкретный день (ADR 0076). Снаружи берут
 * `@entities/garage` — внутренние модули слайса не видны.
 */
export { garageApi } from './api/garageApi';
export { garageKeys } from './api/keys';
