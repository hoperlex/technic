/**
 * Контрагент: заказчик и исполнитель работ портала (ADR 0010, 0038). Пока слайс держит только
 * ключи запросов — сам справочник ещё живёт в `api/resources.ts` и переедет вместе с ним.
 */
export { counterpartyKeys } from './api/keys';
