import { createQueryKeys } from '@shared/api';

/**
 * Ключи запросов контрагентов.
 *
 * Пока заведён один — справочник для полей выбора: контрагентов спрашивают формы учёток и фильтры
 * журнала, и это один и тот же список. Сырой массив в каждом из мест разошёлся бы на элемент, и
 * два экрана держали бы в кэше два одинаковых справочника.
 */
export const counterpartyKeys = createQueryKeys('counterparties', {
  options: () => ['options'],
});
