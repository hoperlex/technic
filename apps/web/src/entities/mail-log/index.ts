/**
 * Журнал отправки писем (ADR 0199): очередь `mail_messages` глазами администратора — когда письмо
 * составлено, по какой причине, кому и чем кончилась доставка.
 *
 * Своя сущность, а не часть рассылок: расписание и адресаты отвечают на вопрос «как должно быть»,
 * журнал — «как оказалось». Общего у них только раздел администрирования и право доступа.
 */
export { mailLogApi } from './api/mailLogApi';
export { mailLogKeys } from './api/keys';
