/**
 * Техобслуживание техники по пробегу (план «Показания техники», Р10—Р14): когда машину
 * обслуживали, сколько она прошла с тех пор и чем это подтверждено.
 *
 * Слайс свой, а не часть показаний, потому что своим у этих данных всё: право
 * (`vehicleMaintenance.read/write`), ручки, кэш и потребители. Экранов у них три — блок в
 * карточке машины, окно сводки из строки списка техники (справочник и гараж) и колонка гаража, —
 * и клиент, живший бы рядом с одним из них, второму был бы соседом по слою.
 *
 * Компонентов здесь нет: блок и форма знают про право текущего пользователя и про загрузку файлов,
 * а слой сущностей ни того, ни другого не знает — они живут сценарием
 * (`features/vehicle-maintenance`).
 *
 * Снаружи берут `@entities/vehicle-maintenance` — внутренние модули слайса не видны.
 */
export { vehicleMaintenanceApi, type MaintenanceSnapshotDto } from './api/vehicleMaintenanceApi';
export { vehicleMaintenanceKeys } from './api/keys';
