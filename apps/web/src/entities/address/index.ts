/**
 * Адрес площадки и точек маршрута.
 *
 * Слайс заведён вокруг одного поля, и это не мелочь: поле не просто подсказывает строку, а
 * превращает ответ DaData в `AddressMeta` — доменную запись о том, насколько адрес опознан
 * (ADR 0006). От неё зависит, покажет ли портал галочку «адрес найден» и можно ли строить по нему
 * маршрут. Транспорт подсказок общий и живёт в `shared/api`; смысл ответа — здесь.
 */
export { AddressAutoComplete, AddressCell } from './ui/AddressAutoComplete';
