import { queryOptions } from '@tanstack/react-query';
import type { CounterpartyDto } from '@technic/contracts';
import { apiFetch, type ListResult } from '@shared/api';
import { DICTIONARY_PAGE_SIZE } from '@shared/config';
import { serviceCompanyKeys, serviceExecutorKeys } from './keys';

/**
 * Сервисные компании для выбора исполнителя (Р1: контрагент типа `service`).
 *
 * Запрос описан здесь, а не на экранах: его спрашивают дважды — фильтр списка «сервис» и окно
 * назначения. Разойдись у них `pageSize` или сортировка, один и тот же ключ отдавал бы двум
 * местам разные перечни.
 *
 * Только действующие: назначить заявку приостановленному контрагенту нельзя (сервер откажет), а
 * у уже назначенных наименование приходит снимком в самой заявке.
 *
 * Перечень спрашивается прямым `apiFetch`, а не ручками `@entities/counterparty`, и появление
 * слайса контрагентов этого не отменило: слайс — сосед по слою, а импорт сущности из сущности
 * разметка границ запрещает. Разрешённое направление там ровно одно (оба вида заявок берут общее
 * из `entities/request`), и заводить второе ради одного справочника дороже, чем спросить его
 * самому. `@shared/api` лежит ниже слоя сущностей — `apiFetch` оттуда не обход правила, а тот
 * общий словарь, которым слою и положено пользоваться.
 *
 * Поэтому «исправление» этого места на `counterpartiesApi` роняет линт границ: зелёным оно станет
 * только вместе с новой точечной политикой в `eslint.config.mjs` — ценой второго разрешённого
 * направления между сущностями. Ключ `serviceCompanyKeys` остаётся здесь по той же причине.
 */
export const serviceCompanyOptionsQuery = () =>
  queryOptions({
    queryKey: serviceCompanyKeys.options(),
    queryFn: () =>
      apiFetch<ListResult<CounterpartyDto>>('/counterparties', {
        query: {
          page: 1,
          pageSize: DICTIONARY_PAGE_SIZE,
          type: 'service',
          isActive: 'true',
          sortBy: 'name',
          sortOrder: 'asc',
        },
      }),
    select: (r) => r.items.map((c) => ({ value: c.id, label: c.name })),
  });

/**
 * Кандидаты в поимённые исполнители (Н5, Н6): учётки, которых **можно** назначить на заявку.
 *
 * Отбор — право `serviceRequests.execute`, и он не украшение: сервер проверяет его при назначении
 * и отвечает 422 с именем человека (`resolveNamedExecutors`). Предложи портал учётку без
 * полномочия — выбор оказался бы отказом уже после нажатия «Назначить».
 *
 * Фильтр стоит на клиенте, потому что перечень учёток фильтра по праву не имеет: список приходит
 * одной страницей справочника, и это тот же ответ, которым живут фильтры журнала и выдача
 * полномочий.
 *
 * **Перечень учёток портал отдаёт только `users.manage`** — то есть администратору. У «Ведения» и
 * «ИТ-службы», которые и назначают, такого права нет, и до появления серверной ручки «кандидаты в
 * исполнители» поимённый слой виден им только тем составом, что уже стоит в заявке. Поэтому
 * спрашивать этот перечень имеет смысл лишь там, где он разрешён (`enabled` у вызывающего), а
 * само поле обязано показывать назначенных из самой заявки, а не из этого ответа.
 */
/**
 * Кандидаты в поимённые исполнители ЭТОЙ заявки — своей ручкой модуля, а не отбором из списка
 * учёток.
 *
 * `GET /users` закрыт правом `users.manage`, которого нет ни у «Ведения», ни у ИТ-службы: поле
 * выбора заполнялось бы только у администратора портала, а у того, кто заявки и распределяет,
 * оставалось бы пустым. Ручка модуля закрыта тем самым правом, которым назначают
 * (`serviceRequests.assign`), и отбирает держателей `serviceRequests.execute` по **эффективным**
 * правам — то есть с гейтом совместимости набора с ролью.
 *
 * Заявка в запросе обязательна (план аудита исполнителей, Р7): сервер отвечает не «кого вообще
 * можно назначить», а «кто сможет работать по ЭТОЙ заявке», — тем же предикатом, которым
 * пригодность проверяет само назначение. Список поэтому и не предлагает «мёртвых» кандидатов:
 * решает это сервер, а не окно.
 */
export const serviceExecutorCandidatesQuery = (requestId: string) =>
  queryOptions({
    queryKey: serviceExecutorKeys.options(requestId),
    queryFn: () =>
      apiFetch<{ items: { id: string; fullName: string }[] }>(
        `/service-requests/executor-candidates?requestId=${requestId}`,
      ),
    select: (r) => r.items.map((u) => ({ value: u.id, label: u.fullName })),
  });
