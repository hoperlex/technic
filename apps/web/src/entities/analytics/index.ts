/**
 * Сводная аналитика по заказчикам: работа заказа техники, вывоза мусора и механизации за период
 * в разрезе «объект / отдел» (`docs/analytics-summary-export-plan.md`).
 *
 * Слайс, а не клиент рядом со вкладкой выгрузки: свод — общий счёт трёх модулей, и потребителей у
 * него будет двое (книга Excel и экран аналитики, Р14). Клиент, оставленный в `pages/admin`,
 * второму потребителю оказался бы соседом по слою — импортом, который границы запрещают, — и
 * экран завёл бы себе вторую копию.
 *
 * Снаружи берут `@entities/analytics` — внутренние модули слайса не видны.
 */
export { analyticsApi } from './api/analyticsApi';
