/**
 * Автозапчасти: склад гаража — что лежит на складе, сколько и к чему подходит (план
 * `docs/auto-parts-plan.md`).
 *
 * Слайс свой, а не часть гаража: у склада своя ручка, свои два права (`autoParts.manage`,
 * `autoParts.stock`), свой кэш и своя жизнь — остаток не про день среза, которым живут остальные
 * вкладки. Потребителей у него двое, и это второй довод: вкладка «Автозапчасти» и форма акта
 * обслуживания, где деталь списывают на машину (Р4). Клиент, живший бы рядом с одной из них,
 * второй был бы соседом по слою — а соседей слой сущностей друг другу не показывает.
 *
 * Компонентов здесь нет: и вкладка, и её окна знают про права смотрящего и про адрес страницы, а
 * слой сущностей ни того, ни другого не знает.
 *
 * Снаружи берут `@entities/auto-part` — внутренние модули слайса не видны.
 */
export { autoPartApi } from './api/autoPartApi';
export { autoPartKeys, autoPartRefKeys } from './api/keys';
