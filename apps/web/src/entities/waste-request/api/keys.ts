import { createQueryKeys } from '@shared/api';

/**
 * Ключи заявок «Вывоз мусора».
 *
 * Пока здесь только корень, и это не заготовка на будущее. Сами запросы — список, сводка, карточка,
 * группы контейнеров — по-прежнему собираются строками в `pages/WasteRequestsPage.tsx`: слайс
 * заявок на вывоз ещё не разрезан (этап 2 FSD, `docs/frontend-fsd-stage-2.md`). А вот корень нужен
 * УЖЕ СНАРУЖИ страницы: ячейка колонки «Талоны» подтверждает талоны прямо из строки и обязана
 * перечитать список, не зная ни его отбора, ни номера страницы.
 *
 * Своя копия строки `['waste-requests']` в фиче означала бы, что два файла договариваются о ключе
 * молча — ровно та беда, ради которой заведена фабрика: промах в букве не ломает ничего заметного,
 * список просто не обновится, и увидят это на экране, а не в тестах.
 */
export const wasteRequestKeys = createQueryKeys('waste-requests', {});
