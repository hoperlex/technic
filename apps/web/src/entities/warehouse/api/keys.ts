import { createQueryKeys, type Query } from '@shared/api';

/**
 * Ключи запросов складов.
 *
 * Видов запроса два. Страница вкладки: поставщик, активность и поиск по адресу приходят в
 * параметрах. И весь список действующих складов — для выбора адреса погрузки или разгрузки
 * (ADR 0069): склад стал местом, куда едет машина, а не только строкой справочника. Под общим
 * ключом форма получила бы отфильтрованную страницу таблицы, а таблица — весь справочник мимо
 * своих фильтров.
 */
export const warehouseKeys = createQueryKeys('warehouses', {
  list: (params: Query) => ['list', params],
  options: () => ['options'],
});
