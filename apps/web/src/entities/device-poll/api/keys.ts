import { createQueryKeys } from '@shared/api';

/**
 * Ключи опроса по сети.
 *
 * СВОЙ КОРЕНЬ, отдельно от телеметрии карточки и от очереди писем. Предмет здесь — не показания
 * аппарата и не ничейные письма, а СОСТОЯНИЕ СВЯЗИ: дошли ли мы до адреса и что он ответил. Под
 * общим корнем нажатие «получить данные» гасило бы блоки показаний во всех открытых карточках, а
 * разбор письма — перечитывал бы служебный список целей.
 */
export const devicePollKeys = createQueryKeys('device-poll', {
  targets: () => ['targets'],
});
