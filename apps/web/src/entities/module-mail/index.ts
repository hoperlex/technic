/**
 * Служебные адресаты писем модулей: ящик службы, за которым нет учётной записи (план
 * `docs/office-equipment-mail-and-history-plan.md`, Р64).
 *
 * Своя сущность, а не часть рассылок: расписание отвечает на вопрос «кому из учётных записей и
 * когда уходит сводка», а здесь — «на какой адрес уходит письмо по событию». Общего у них только
 * раздел администрирования и право доступа.
 *
 * Рядом с адресатами живут **рубильники событий** (план
 * `docs/office-equipment-mail-expansion-plan.md`, §5.1): адресаты отвечают, кому уходит копия, а
 * рубильник — уходит ли письмо по событию вообще. Одна сущность на двоих потому, что настройка одна
 * и правит её один человек в одном разделе; ручки, ключи и таблицы у них при этом разные.
 */
export { moduleMailApi } from './api/moduleMailApi';
export { moduleMailEventsApi } from './api/moduleMailEventsApi';
export { moduleMailKeys, moduleMailEventKeys } from './api/keys';
