import { createQueryKeys } from '@shared/api';

/**
 * Ключи служебных адресатов писем (план `docs/office-equipment-mail-and-history-plan.md`, Р64).
 *
 * Семейство одно: список приходит целиком — строк здесь единицы, и вкладку открывают, чтобы
 * увидеть настройку полностью. Страниц и фильтров у него нет, поэтому и параметров в ключе нет:
 * добавь их «на будущее» — и первый же запрос без параметров промахнулся бы мимо кэша.
 */
export const moduleMailKeys = createQueryKeys('module-mail-recipients', {
  list: () => ['list'],
});

/**
 * Ключи рубильников событий (план `docs/office-equipment-mail-expansion-plan.md`, §5.1).
 *
 * Корень свой, а не семейство внутри адресатов: строки отвечают на разные вопросы («кому уходит
 * копия» и «уходит ли письмо вообще»), правятся разными ручками и живут в разных таблицах. Общий
 * корень означал бы, что правка одного адреса гасит список рубильников — лишний запрос при каждом
 * сохранении и мигающая таблица, которой ничего не сделали.
 *
 * Семейство одно и без параметров по той же причине, что у адресатов: событий семь, реестр закрыт
 * кодом, страниц и фильтров у списка нет и быть не может.
 */
export const moduleMailEventKeys = createQueryKeys('module-mail-events', {
  list: () => ['list'],
});
