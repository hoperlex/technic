/**
 * Недельная заявка на технику (ADR 0085): документ-основание над заказами ТС — площадка заказывает
 * не машину на срок, а неделю целиком. Пока слайс держит только ключи запросов: сами ручки живут в
 * `api/resources.ts` и переедут вместе с ним.
 */
export { weeklyRequestKeys } from './api/keys';
