/**
 * Числа каркаса кабинета водителя (план docs/driver-cabinet-ux-plan.md, Р1).
 *
 * Пороги предупреждений при вводе показаний отсюда уехали в слайс показаний
 * (`@entities/vehicle-reading`): их зовут два экрана — кабинет и карточка отчёта в гараже, где
 * персонал вносит показания за работника (ADR 0103, Р26), — а два экрана одного слоя друг друга не
 * видят. Здесь остаётся то, что относится к самому кабинету и никому больше.
 */

/**
 * Масштаб шрифта кабинета. Экран читают с телефона в кабине, на солнце и в перчатках, поэтому
 * величина одна на весь каркас: подобрать её на живом телефоне — работа одного значения, а не
 * поиска по файлам (Р1).
 */
export const DRIVER_FONT_SCALE = 1.15;
