/**
 * Отметка выдачи и её снятие (план `docs/mechanization-module-plan.md`, Р2): момент, с которого
 * техника стоит на площадке и стоит денег, и единственное лечение ошибки в нём.
 *
 * Одним слайсом, а не двумя: это одно и то же поле `actual_from` с двух сторон, и оба окна
 * обязаны говорить об одном и том же дне одними словами. Разъехавшись, они назвали бы снятие
 * отметки «отменой выдачи» — то есть тем, чем оно не является.
 */
export { MechIssueModal } from './ui/MechIssueModal';
export { MechRevokeIssueModal } from './ui/MechRevokeIssueModal';
