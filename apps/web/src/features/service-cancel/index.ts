/**
 * Отмена одной заявки на обслуживание (Р10 плана
 * `office-equipment-card-and-list-cleanup-plan.md`): обязательная причина, а у ремонта — решение
 * «что делаем вместо» и пометка «рекомендована замена аппарата».
 *
 * Своим слайсом, а не подписями над общим `ReasonModal`: у окна появилось содержание сверх причины,
 * и оно доменное — вид заявки решает состав полей, а галочка делает решение обязательным. Общее
 * окно причины при этом живёт дальше: им пользуются вывоз мусора, заказ ТС и заявки на регистрацию.
 *
 * Сценарий, а не сущность: окно связывает заявку, её перечень потерь и один переход.
 */
export { ServiceCancelModal } from './ui/ServiceCancelModal';
