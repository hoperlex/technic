/**
 * Продление аренды (план `docs/mechanization-module-plan.md`, Р9, Р11): новая дата возврата и
 * обязательная причина.
 *
 * Свой слайс, а не пункт внутри выдачи: продление живёт **своим правом** (`mechRequests.extend`,
 * у диспетчера) и своим событием истории, то есть отвечает на другой вопрос и другому человеку.
 * Сложить его к отметке выдачи значило бы, что два действия с разными правами разъезжаются по
 * одному окну — и первое же изменение прав переписывало бы оба.
 */
export { MechExtendModal } from './ui/MechExtendModal';
