/**
 * Тупик «нужной техники нет в справочнике» в форме заявки на обслуживание (этап 7, Р40): у кого
 * есть право вести справочник — заведение карточки поверх формы, у остальных — обращение в
 * техподдержку с готовым контекстом. Сценарий, а не сущность: слайс знает и право смотрящего, и
 * то, что заведённая единица тут же становится значением поля заявки.
 *
 * Наружу отдаётся один вход — ссылка под полем: оба ответа на один вопрос обязаны жить рядом,
 * иначе следующая правка добавит третий, не заметив двух прежних.
 */
export { EquipmentNotFoundLink } from './ui/EquipmentNotFoundLink';
