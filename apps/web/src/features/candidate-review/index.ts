/**
 * Проверка сообщений о технике, которой нет в справочнике (план
 * `docs/office-equipment-candidate-plan.md`, Р12–Р15).
 *
 * Сценарий, а не сущность: слайс знает, что проверка кончается ЗАПИСЬЮ В ПАРК — и потому гасит
 * ключи парка, моделей, расходников и заявки тем же набором, что и быстрое создание карточки.
 * Сама сущность кандидата (ручки, ключи, состояние предмета в заявке) живёт в
 * `@entities/office-equipment-candidate` и о решениях не знает.
 *
 * Наружу отдаётся одно окно: три решения — стороны одной работы, и разложенные по разным входам
 * они разъехались бы на инвалидации.
 */
export { CandidateReviewModal } from './ui/CandidateReviewModal';
