/**
 * Ввод адреса: подсказки DaData и выбор из справочника объектов и складов (ADR 0069).
 *
 * Сценарий, а не сущность, и потому слой `features`: поле знает сразу о трёх справочниках —
 * объектах, складах и площадках отделов, — а слайсу `entities` соседи по слою не видны. Домен
 * адреса при этом остаётся в `entities/address`: там `AddressMeta`, подсказки и значок
 * верификации; здесь — то, как из них собирается поле формы.
 */
export { AddressField } from './ui/AddressField';
