/**
 * Траты на запчасти в карточке машины (план `docs/auto-part-receipts-plan.md`, Р16).
 *
 * Сценарий, а не сущность: здесь знают про право смотрящего (`garage.read` решает, будет ли вообще
 * запрос) — а слой сущностей про права не знает. Данные приходят из `@entities/auto-part-receipt`.
 *
 * Своего окна у сценария нет. Перечень покупок показывает `pages/garage/VehiclePartsSpendModal`:
 * оно одно на три входа (Р15), и живёт там же, где два других входа, — а сценарий получает только
 * адрес, по которому оно откроется.
 */
export { VehiclePartsSpendBlock } from './ui/VehiclePartsSpendBlock';
