/**
 * Правка факта выдачи расходников (Р6 набросков, план §6.2): сколько на самом деле выдали по
 * строкам заявки. Слой `features` — сценарий стороны исполнителя: окно знает и строки заявки, и
 * правило «событие журнала пишется на разницу».
 *
 * Отдельно от закрытия работ, хотя величина у них одна: закрытие — это переход, у которого
 * списание лишь одно из шести действий транзакции, а здесь склад двигают, не трогая статус. Одно
 * окно на два случая означало бы кнопку «Закрыть работы», которая иногда ничего не закрывает.
 */
export { ServiceConsumablesIssueModal } from './ui/ServiceConsumablesIssueModal';
