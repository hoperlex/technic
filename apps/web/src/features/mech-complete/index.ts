/**
 * Завершение аренды (план `docs/mechanization-module-plan.md`, Р7): реальные даты, отработанные
 * часы или смены и итоговая стоимость со счётом арендодателя.
 *
 * Сценарий, а не сущность: окно знает ставку заявки, формулу расчёта и то, почему сумма имеет
 * право с ней не сойтись. Переход в «Выполнена» уходит той же ручкой, что и факт, — порознь их
 * пришлось бы проводить не атомарно.
 */
export { MechCompleteModal } from './ui/MechCompleteModal';
