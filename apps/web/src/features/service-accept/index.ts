/**
 * Приёмка работ и возврат на доработку (ADR 0085, §9.3) — два ответа оператора на предъявленный
 * исполнителем факт. Сценарий, а не сущность: окно знает и итог закрытия, и нехватку закрывающих
 * документов, и то, что возврат стирает у заявки.
 */
export { ServiceAcceptModal, type AcceptMode } from './ui/ServiceAcceptModal';
