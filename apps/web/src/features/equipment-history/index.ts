/**
 * История единицы оргтехники: три бизнес-блока и полная лента (план
 * `docs/office-equipment-history-blocks-plan.md`, Р11).
 *
 * Сценарий, а не сущность: окно знает правило показа («вкладка заявок — только при праве заявок»)
 * и открывается из трёх мест — справочника, где карточку ведут, раздела «Орг.техника», где технику
 * эксплуатируют, и секции «Обслуживание и гарантии» в карточке, которая показывает первые пять
 * строк того же блока заявок (Р7).
 */
export { EquipmentHistoryModal, type EquipmentHistoryBlock } from './ui/EquipmentHistoryModal';
/**
 * Строка блока заявок для секции карточки: тот же блок, та же строка, первые пять (К7). Наружу
 * отдаётся именно строка, а не второй список: три места, рассказывающие про заявки по-разному, —
 * это ровно та находка (Н1), ради которой план и написан.
 */
export { EquipmentRequestLine } from './ui/requestRow';
export {
  REQUESTS_EMPTY_TEXT,
  REQUESTS_PREVIEW_LIMIT,
  useEquipmentRequestsPreview,
} from './ui/RequestsBlock';
