/**
 * Заморозка заявки на обслуживание (Р103): отложить с причиной и вернуть в работу. Сценарий, а не
 * сущность — окно знает и обязательность причины (Р107), и то, что дуга назад одна: заявка
 * возвращается ровно в тот статус, из которого её отложили (Р104), а выбирать цель нельзя.
 */
export { ServiceHoldModal, type HoldMode } from './ui/ServiceHoldModal';
