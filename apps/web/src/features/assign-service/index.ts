/**
 * Назначение исполнителей заявки оргтехники (Н5, Н6) — действие «Ведения» и ИТ-службы, то есть
 * держателей права `serviceRequests.assign`. Сценарий, а не сущность: окно знает и заявку, и оба
 * слоя исполнителей — сотрудников поимённо и сервисные компании строкой, — и цену замены (у
 * снятого исполнителя стирается объём работ, а заявка из «В работе» возвращается в «Новую», Р5),
 * поэтому слой `features`.
 */
export { AssignServiceModal } from './ui/AssignServiceModal';
