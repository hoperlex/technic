/**
 * Назначение исполнителей заявки оргтехники (Н5, Н6) — действие того, кто ведёт модуль или
 * визирует от ИТ. Сценарий, а не сущность: окно знает и заявку, и оба слоя исполнителей —
 * сотрудников поимённо и сервисные компании строкой, — и цену замены (у снятого исполнителя
 * стирается смета), поэтому слой `features`.
 */
export { AssignServiceModal } from './ui/AssignServiceModal';
