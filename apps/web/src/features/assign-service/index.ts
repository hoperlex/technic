/**
 * Назначение и переназначение сервисной компании (ADR 0085, §5.3) — действие оператора
 * оргтехники. Сценарий, а не сущность: окно знает и заявку, и перечень контрагентов-исполнителей,
 * и цену замены (у прежнего сервиса стирается смета), — поэтому слой `features`.
 */
export { AssignServiceModal } from './ui/AssignServiceModal';
