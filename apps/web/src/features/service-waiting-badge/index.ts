/**
 * Бейдж «ждёт меня» на разделе оргтехники (ADR 0085, Р39; виза ИТ — Р51).
 *
 * Слайс сценария, а не хук рядом с каркасом: правило «кому счётчик вообще спрашивать» — это знание
 * о цикле заявки, а не о вёрстке меню. Каркас портала (`AppLayout`) спрашивает число и рисует
 * кружок; кто в цикле стоит, решает здесь.
 */
export { useServiceWaitingCount } from './model/useServiceWaitingCount';
