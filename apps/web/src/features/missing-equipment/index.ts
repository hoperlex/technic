/**
 * Тупик «нужной техники нет в справочнике» в форме заявки на обслуживание (этап 7, Р40; план
 * `docs/office-equipment-candidate-plan.md`, §9).
 *
 * Слайс переименован из `quick-create-equipment` вместе с третьей ветвью: имя называло ОДИН из
 * ответов, а предмет здесь — сам тупик и все выходы из него. Ответов стало три — завести карточку,
 * сообщить об аппарате, написать в поддержку, — и слайс с именем первого из них читался бы как
 * «быстрое создание, к которому зачем-то приделали чужие окна».
 *
 * Сценарий, а не сущность: слайс знает и права смотрящего, и то, что заведённая единица тут же
 * становится значением поля заявки, а заявленный аппарат — черновиком в её теле.
 *
 * Наружу отдаются вход и тип черновика: оба ответа на один вопрос обязаны жить рядом, иначе
 * следующая правка добавит четвёртый, не заметив трёх прежних.
 */
export { EquipmentNotFoundLink } from './ui/EquipmentNotFoundLink';
export { equipmentCandidateDraft, type EquipmentCandidateDraft } from './model/draft';
