/**
 * Блок карточки «Показания и события» (план
 * `docs/office-equipment-mail-telemetry-plan.md`, §10).
 *
 * Сценарий, а не сущность: блок знает правило показа времени («время аппарата, приём — только
 * когда времени аппарата нет») и объясняет свою пустоту словами. Данные ему даёт слайс
 * `@entities/device-telemetry`.
 */
export {
  DeviceTelemetryBlock,
  TELEMETRY_EMPTY_TEXT,
  /**
   * Печать числа показания наружу — ради своего теста, а не ради второго читателя: правило «без
   * хвоста нулей» проверяется на строках, которых в блоке не собрать глазами (восемнадцать знаков
   * счётчика).
   */
  formatMetricValue,
} from './ui/DeviceTelemetryBlock';
