/**
 * Договорённость с арендодателем (план `docs/mechanization-module-plan.md`, Р6, Р7): у кого берём
 * технику, почём и за что.
 *
 * Сценарий, а не сущность, и потому слой `features`: окно складывает справочник контрагентов двух
 * типов, переход заявки в работу и необязательную отметку выдачи — то есть знает правила модуля,
 * а не форму записи.
 */
export { MechTakeInWorkModal } from './ui/MechTakeInWorkModal';
