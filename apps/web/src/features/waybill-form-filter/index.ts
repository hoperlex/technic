/**
 * Отбор списка по бланку работы дня (план «Срезы дня», Р5–Р7, Р20).
 *
 * Сценарий, а не сущность: своих данных у фильтра нет — он спрашивает у списка тот же ответ,
 * который сервер считает о строке (`busyWaybillForms`), и знает лишь, как этот вопрос выглядит на
 * десктопе и в шите телефона.
 *
 * Потребителей двое, и оба задают один вопрос: гараж → «Водители» (чем закрывается день человека)
 * и «Заказ ТС» → «На объекте» (чем закрывается день заявки). Вкладка гаража «Техника» его не
 * спрашивает вовсе — там ключ `forms` запрещён схемой (Р20), и слать его туда портал не должен.
 */
export { useWaybillFormFilter } from './ui/useWaybillFormFilter';
