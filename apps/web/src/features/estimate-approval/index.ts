/**
 * Отказ по объёму работ (ADR 0085, §9.3; цикл заявки — Р8, Р11, Р12) — исход «не согласовано» и
 * только он: причина, решение и пометка «рекомендована замена аппарата», после которых заявка
 * уходит в «Отменена» (В1).
 *
 * Согласие через это окно не проходит вовсе. Содержания у него нет — есть ревизия и сумма, которые
 * человек только что видел, — и оно идёт подтверждением прямо из набора действий: держи окно оба
 * исхода, пункт «Не согласовано» открывал бы окно с вопросом «согласовать или нет».
 *
 * Сценарий, а не сущность: окно связывает заявку, её строки, приложенные документы и один исход.
 */
export { EstimateApprovalModal } from './ui/EstimateApprovalModal';
