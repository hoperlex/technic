/**
 * Закрытие работ исполнителем (ADR 0085, §9.3): отметки факта по строкам сметы, скидка по акту и
 * вычисляемый итог. Сценарий стороны сервиса — слой `features`: окно знает и смету заявки, и
 * правило «больше согласованного не закрывают».
 *
 * Режимов у окна три, а не два (Р6 плана `office-equipment-card-and-list-cleanup-plan.md`):
 * ремонт подрядчика закрывается фактом по строкам, расходники — отметкой выдачи, внутренний
 * ремонт — датой и необязательным «Что сделали». Третий выбирает признак контрактов
 * `serviceRequestNeedsEstimate`, а не наличие строк: историческая смета переданной своему заявки
 * остаётся историей и фактом не становится.
 */
export { ServiceCompleteModal } from './ui/ServiceCompleteModal';
