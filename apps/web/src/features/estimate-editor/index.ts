/**
 * Редактор объёма работ (ADR 0085, §9.3; цикл заявки — Р9): состав ремонта, цены, сроки гарантии и
 * предъявление на согласование. Сценарий стороны сервиса — отсюда и слой `features`: окно знает и
 * заявку, и правило «предъявить можно только сохранённое», и режим гарантийного ремонта без
 * оплаты, и оба замка непогашенного предъявления — правку состава и повторное предъявление.
 *
 * ТРЕТИЙ СПОСОБ ПОДАЧИ — СЧЁТОМ (план
 * `docs/office-equipment-on-site-and-invoice-estimate-plan.md`, Р2 и Р10). Ревизия собирается из
 * приложенного документа: ни строк, ни суммы у неё нет — их даст разбор счёта, — поэтому окно
 * УБИРАЕТ с экрана таблицу граф, переключатель режимов, описание и стоимость, а не гасит их:
 * погашенный режим у окна уже занят замком висящего предъявления и означает другое. Рядом живёт
 * заявление об освобождении от подписи — «Согласование не требуется» (Р1, Р3): чекбокс законен и
 * при построчном предъявлении, показывается только тому, кому его разрешает предикат контрактов, и
 * оба способа видны, лишь пока включены свои рубильники.
 *
 * ДВА РЕЖИМА ВВОДА И ДВЕ ДВЕРИ (план
 * `docs/office-equipment-free-estimate-and-executor-scope-plan.md`, Р1, Р2, Р8). Набирают состав
 * либо по графам, либо одной свободной записью — перечнем из письма подрядчика с общей стоимостью;
 * в базе это одинаковые строки, признака формата там нет. Дверей тоже две: исполнитель правит свой
 * черновик, «Ведение» раскладывает присланный перечень по графам (`intent="breakdown"`), и по
 * согласованной ревизии вторая ручка переиздаёт документ — заявка уходит за новой подписью.
 *
 * ADR 0208 makes the external executor's normal door document-only. The service-company operator
 * enters through `work_done`, which submits the existing exemption declaration and keeps the modal
 * open for the closing act; named executors use `document` and retain human approval.
 */
export { EstimateEditorModal } from './ui/EstimateEditorModal';
export type { EstimateEditorIntent } from './model/useEstimateEditor';
