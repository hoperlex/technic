// ── Рубильники приёма (план `docs/office-equipment-request-subject-plan.md`, Р10) ──
//
// Ключ, которым ВЫКАТ отделён от ВКЛЮЧЕНИЯ. Код едет закрытым, а открывает его строка в базе —
// `UPDATE`, а не релиз. Заведено ради одной находки ревью (Р11): миграция прав применяется до
// перезапуска приложения, и без рубильника выдача права немедленно открыла бы СТАРУЮ реализацию,
// которая ни о каком включении не знает.
//
// ЭТО НЕ ПОЛНЫЙ РЕЗОЛВЕР ФЛАГОВ, и называть его так нельзя. План кандидата (§14) требовал общего
// механизма — с пилотными областями, аудитом переключения и административной ручкой; его в
// репозитории нет, и эта волна его не пишет. Здесь ровно один вопрос — «включён ли ключ вообще», —
// одинаковый для всех учёток. Отвергнуто вслух: (1) ждать общий резолвер — волна встала бы на
// внешней задаче, а выкатывать её без серверного fail-closed нельзя вовсе; (2) собрать пилотные
// области «на вырост» — механизм без единого читателя расходится с будущими требованиями быстрее,
// чем пишется; (3) `process.env`/`VITE_*` — сборочная константа не выключается аварийно и разъезжается
// между сервером и порталом на любом частичном выкате.

/**
 * Реестр ключей. Закрыт намеренно: перечень обязан совпадать с `CHECK` таблицы `feature_flags`
 * (миграция 0293) — иначе опечатка в ключе выглядела бы настройкой («рубильник включён»), пока
 * настоящий ключ сидит без строки и молчит.
 *
 * Новый ключ приходит со своей строкой, то есть с миграцией: бесплатным этот реестр не бывает и
 * не должен — рубильник без строки в базе означает «выключён навсегда», а это состояние обязано
 * быть видно на ревью выката, а не в проде.
 */
export const FEATURE_FLAGS = [
  /**
   * Приём сообщений о технике, которой нет в справочнике (план
   * `docs/office-equipment-candidate-plan.md`, §14; ADR 0165).
   *
   * Гасит ВХОД, а не разбор: `POST /service-requests` с `equipmentCandidate` при выключенном ключе
   * отвечает отказом даже держателю `officeEquipment.propose`, а очередь проверки, чтение уже
   * созданных кандидатов и три решения ключом не ограждаются. Аварийное выключение обязано
   * прекратить приток, не заперев то, что уже принято: разобрать накопленное всё равно кто-то
   * должен.
   */
  'office_equipment_candidate_intake',
  /**
   * Сужение области исполнителя в заявках оргтехники (план
   * `docs/office-equipment-free-estimate-and-executor-scope-plan.md`, Р3, Р4 и Р7; ответы В3 и В6
   * заказчика от 09.09.2026: «исполнитель видит только свои заявки»).
   *
   * ЧТО ВКЛЮЧАЕТ. Одну развилку, стоящую ПЕРВОЙ и в области чтения, и в области действий: держателю
   * набора ИТ-службы либо набора исполнителя — и никакого другого набора модуля — заявка
   * открывается по правилу «назначен ∨ автор ∨ след снятия» вместо сквозной области модуля.
   * Остальных ключ не касается ни одной веткой: «Ведение», администратор, роли-заказчики и
   * подрядчик работают в точности как прежде. Развилка, а не добавка: `and` с осью роли вернул бы
   * такому субъекту «свою площадку», то есть заявки, к которым он отношения не имеет.
   *
   * ВЫКЛЮЧЕННОЕ СОСТОЯНИЕ = СЕГОДНЯШНЕЕ ПОВЕДЕНИЕ, буквально: сисадмин видит заявки всей компании
   * по сквозной области ИТ-набора, и до развилки предикаты не доходят вовсе. Поэтому код едет
   * выключенным, а открывает его строка в базе: сужение области замечают все держатели профиля
   * разом и в один день, и откат обязан быть `UPDATE`, а не обратным выкатом.
   *
   * ПОЧЕМУ КЛЮЧ ВООБЩЕ ЗАВЁДЕН, А НЕ «СНЯЛИ КАРТУ ОБЛАСТИ И ГОТОВО». Первая редакция плана убирала
   * `serviceRequests` из карты сквозной области тем же выпуском — и тем обессмысливала рубильник:
   * выключенный ключ не вернул бы прежнее поведение, потому что карта уже сужена самим выкатом.
   * Порядок поэтому обратный: сперва ключ и ветки за ним (выпуск A), карта уезжает вторым выпуском
   * (B), сам ключ убирается третьим (C).
   *
   * Строка в базе едет своей миграцией — вместе с правкой `CHECK` таблицы `feature_flags`: реестр
   * закрыт и обязан совпадать с ним (см. абзац выше про то, что бесплатным этот реестр не бывает).
   */
  'service_request_executor_scope',
  // ── Объём работ документом и освобождение от подписи: ДВА ключа, а не один ──
  //
  // ПОЧЕМУ РАЗДЕЛЕНЫ. Подача счётом полезна и при обычном согласовании: исполнитель прикладывает
  // документ, а подпись ставит «Ведение» руками — законный сценарий (план
  // `docs/office-equipment-on-site-and-invoice-estimate-plan.md`, Р2), и обхода подписи он не
  // требует. Обход — отдельное продуктовое решение, у которого нет ни лимита, ни политики по
  // контрагенту (Р13), и цена ошибки у него денежная, а не интерфейсная. Включать их вместе
  // незачем, а ВЫКЛЮЧАТЬ ПО ОТДЕЛЬНОСТИ обязательно: единый ключ гасил бы приём документов вместе
  // с автоподписью, то есть ломал бы работающий сценарий ради отключения другого.
  //
  // На этом держится порядок выката из четырёх шагов (§7): выпуск читателей → проверка, что старых
  // узлов не осталось → подача документом → освобождение. Слитый ключ сделал бы шаги 3 и 4 одним и
  // открыл бы автоподпись тем же `UPDATE`, которым включают документную подачу.
  /**
   * Подача объёма работ документом — счёт вместо граф (Р2 и Р10; §7, шаг 3).
   *
   * ГАСИТ ВХОД, А НЕ ЧТЕНИЕ: при выключенном ключе ручка предъявления не принимает формат
   * `document`, а окно объёма работ не предлагает документный режим. Уже поданные ревизии, планка
   * закрывающего акта, очередь и автозакрытие ключом не ограждаются — иначе аварийное выключение
   * вернуло бы поданным счетам силу закрывающего документа, то есть открыло бы ровно ту дыру, из-за
   * которой §7 и развёл выпуск читателей и включение подачи на разные шаги.
   *
   * ВЫКЛЮЧЕННОЕ СОСТОЯНИЕ = СЕГОДНЯШНЕЕ ПОВЕДЕНИЕ: объём работ предъявляют строками, и до
   * документных веток дело не доходит вовсе.
   */
  'service_estimate_document_mode',
  /**
   * Автопринятие объёма работ по заявлению оператора сервисной компании (Р3 и Р13; §7, шаг 4).
   *
   * ГАСИТ ИСХОД, А НЕ КОМАНДУ, и в этом он не похож на соседей по реестру. Заявление при
   * выключенном ключе не отбивается: предъявление проходит, заявление остаётся в следе, но ожидание
   * подписи открывается как обычно — исход `observed` вместо `applied`. Отказ вместо исхода лишил бы
   * службу единственного способа узнать ДО включения, сколько таких заявлений приходит и на какие
   * суммы.
   *
   * ЭТО ЕДИНСТВЕННЫЙ ПРЕДВАРИТЕЛЬНЫЙ БАРЬЕР У ДЕНЕГ. Ни лимита, ни политики по контрагенту, ни
   * реквизитов счёта заказчик не захотел (Р13), поэтому включённый ключ означает: оператор сервиса
   * вправе автосогласовать любую сумму, а остальные три механизма — письмо офису, тег в списке и
   * спор — видят её только постфактум. Выключение обязано быть `UPDATE`-ом по работающему порталу, а
   * не обратным выкатом.
   */
  'service_estimate_exemption',
] as const;

export type FeatureFlagKey = (typeof FEATURE_FLAGS)[number];

/**
 * Включён ли ключ по ответу сессии — **единственное** правило чтения на портале, и живёт оно рядом
 * с реестром, а не в экране.
 *
 * FAIL-CLOSED ДВАЖДЫ, И ОБА РАЗА ПО РАЗНОЙ ПРИЧИНЕ. Ключа нет в списке — сервер посчитал его
 * выключенным. Списка нет вовсе (`features === undefined`) — отвечает СТАРЫЙ сервер, который о
 * рубильнике не знает: так выглядит окно выката, когда портал уже новый, а приложение ещё не
 * перезапущено. Оба случая обязаны означать «выключено»: `?? true` открыл бы приём на старом
 * ответе — ровно тот сценарий, ради которого рубильник и заводился.
 *
 * Портал НЕ имеет собственной копии значения (env, сборочной константы, локального состояния):
 * второй источник ответа разошёлся бы с серверным молча, и выключенный рубильник показывал бы
 * человеку дверь, за которой стоит 403.
 */
export function hasFeature(
  session: { features?: readonly FeatureFlagKey[] } | null | undefined,
  key: FeatureFlagKey,
): boolean {
  return session?.features?.includes(key) ?? false;
}
