**/node_modules
**/dist
**/.turbo
**/coverage
.git
temp
**/.env
**/.env.*
!**/.env.example
*.log
# Маркер деплоя в корне репо — не должен попадать в build-context (иначе COPY . . инвалидирует кэш)
.deployed-sha
# Отметка о зелёном прогоне ворот качества (ADR 0147) — та же причина, что у .deployed-sha:
# она меняется каждым прогоном и инвалидировала бы кэш сборки, не влияя на сам образ
.quality-check
